[project]
name = "qwen-mm-workspace"
version = "0.1.0"
description = "Development workspace for qwen-mm"
requires-python = ">=3.11,<3.12"
dependencies = []

[dependency-groups]
dev = ["maturin==1.14.1"]

[tool.uv]
package = false

[tool.uv.workspace]
members = [
    "crates/qwen-mm-python",
    "reference",
]
