minimum_pre_commit_version: "4.0.0"
default_install_hook_types: [pre-commit]

repos:
  - repo: local
    hooks:
      - id: ruff-check
        name: ruff check
        entry: ./scripts/with-cargo.sh uv run --locked ruff check --fix
        language: system
        types_or: [python, pyi]
        require_serial: true
      - id: ruff-format
        name: ruff format
        entry: ./scripts/with-cargo.sh uv run --locked ruff format
        language: system
        types_or: [python, pyi]
        require_serial: true
      - id: cargo-fmt
        name: cargo fmt
        entry: ./scripts/cargo.sh fmt --all
        language: system
        files: '(^Cargo\.toml$|^rust-toolchain\.toml$|^scripts/(cargo|with-cargo)\.sh$|\.rs$)'
        pass_filenames: false
      - id: uv-lock
        name: uv lock
        entry: ./scripts/with-cargo.sh uv lock --check
        language: system
        files: '(^|/)(pyproject\.toml|uv\.lock)$|^scripts/(cargo|with-cargo)\.sh$'
        pass_filenames: false
