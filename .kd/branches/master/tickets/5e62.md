---
id: "5e62"
status: closed
deps: [832f, 6b73, 7b4b]
links: []
created: 2026-08-01T00:29:44Z
type: task
priority: 2
closed_at: 2026-08-01T04:05:20Z
assignee: hand
parent: fa8c
---
# B7: Integrate the serial single-request image path

## Goal

Compose the Phase B text and image stages into one public native Rust
single-request processor that matches the full pinned official composed path
for both profiles and has no Python runtime fallback.

## Scope

- Plan visual occurrences in structured-message order, resolve encoded and raw
  RGB inputs, expand image placeholders/ranges, render/tokenize text, prepare
  pixels, and assemble grids plus the complete official conditional output set.
- Support text-only, one-image, multi-image, interleaved, and repeated-reference
  requests inside one request, with deterministic occurrence and patch order.
- Apply B1's validation/error precedence and resource policy across the entire
  operation, including cross-stage counts and materialized-output capacity.
- Add an end-to-end Rust test/candidate entry point that the A3 conformance
  tooling can run without invoking the reference implementation at runtime.

## Non-goals

Do not add multiple-request batching, the two-phase caller-destination API,
PyO3/NumPy ownership, parallel execution, performance claims, video, caching,
or vLLM integration.

## Acceptance Criteria

- [x] Both frozen profiles pass text-only, one-image, interleaved multi-image,
      and repeated-image committed/live differential cases without Python,
      Hugging Face, Qwen VL Utils, Pillow, or TorchVision at runtime.
- [x] Rendered/expanded prompt bytes, output-key presence/order, `input_ids`,
      `attention_mask`, `mm_token_type_ids`, `image_grid_thw`, occurrence order,
      placeholder counts, replacement ranges, shapes, and strides are exact.
- [x] Raw/lossless and lossy encoded `pixel_values` satisfy the immutable
      end-to-end tolerances and grid/patch/placeholder invariants, with A3's full
      numerical diagnostics when a comparison fails.
- [x] Text-only requests omit image outputs; invalid placeholder/media counts,
      malformed structure/options/media, profile drift, resource excess, and
      overflow return the exact stable category in deterministic precedence.
- [x] Repeated references preserve occurrence semantics and output ordering;
      tests prove no occurrence is omitted, reordered, or processed through an
      untracked duplicate stage.
- [x] The serial operation either returns a complete consistent result or an
      error; no partially observable output survives a failed stage.
- [x] A documented offline command runs Phase B end-to-end conformance for both
      profiles, and the full repository correctness suite passes from a clean
      checkout.

## Worklog

### 2026-08-01 — Lord review and adversarial corrections

The first integration review kept B7 open and required a true no-decode media
plan/execute split so aggregate text, grid, patch, and materialized-output
limits win before codec execution. It also moved render-time structural checks
before options/resources, enforced `pixel rows == sum(t*h*w)`, removed the
per-visual tokenizer re-encode by deriving spans from the one full-prompt
encoding, and added a same-processor late-failure atomicity regression.

A second independent audit found three report-only holes. The Lord corrected
the Phase B runner to compare occurrence locations/order, source dimensions,
grid and pixel ranges, complete geometry, trace metadata, and sidecars against
authenticated traversal; to reject non-finite float mismatches with finite
JSON-safe diagnostics; and to emit request/message/content/input plus
grid-t/y/x, patch, channel, temporal, and patch-x/y coordinates. The reviewer
then re-audited the fixes and reported no remaining blocker.

### 2026-08-01 — implementation and verification complete

- Added the public serial `QwenImageProcessor`, compact production result, and
  an explicit same-pass conformance trace that retains prepared RGB without a
  second decode, resize, normalization, or patchification stage.
- Added a self-authenticating five-case corpus for both profiles, including
  padded raw RGB, JPEG, PNG, lossless/lossy WebP, interleaving, and repeated
  references `[1, 0, 1]` with distinct per-occurrence options. Two independent
  regenerations were byte-identical to the committed manifest/source/expected
  files.
- `make phase-b-conformance` passed all 10 profile/case combinations with 62
  comparisons and zero values over bound. All 14 cache-backed core tests passed,
  including six processor regressions and the complete both-profile chat tier.
- `make check` passed strict workspace Clippy, 71 normal Rust tests with 14
  explicit cache-gated tests skipped, example/doc/reference/conformance tests,
  corpus validation, and wheel smoke. Focused example tests, Ruff, formatting,
  and `git diff --check` also passed.

### 2026-08-01 — implementation started

Started after B2, B4, and B6 passed their independent gates and were committed.
Delegated the composed processor implementation plus separate API/conformance
audits to collaboration subagents; the Lord retains ticket state, acceptance
review, integration, verification, and commit. Kingdom peasant workers are not
used.

### 2026-08-01 — ticket created

Scoped from ROADMAP B7 as the Phase B integration gate. B2, B4, and B6 remain
separate prerequisites so text, patch layout, and media semantics are proven
independently before composition.
