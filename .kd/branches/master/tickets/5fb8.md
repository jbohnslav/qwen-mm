---
id: "5fb8"
status: closed
deps: []
links: []
created: 2026-08-27T12:50:02Z
type: task
priority: 2
closed_at: 2026-08-27T13:15:30Z
resolution: completed
closed_context: codex:ad56e22ce3deade5
parent: 7544
---
# Add familiar Qwen model construction and pinned downloads

Add the normal public model-loading entrypoint for supported Qwen profiles.

## Acceptance Criteria

- [x] `Processor.from_pretrained` accepts the canonical `Qwen3` and `Qwen3.5`
      short names plus both exact supported Hugging Face model IDs.
- [x] Missing processor assets are downloaded at the qwen-mm pinned revision through the standard Hugging Face cache without downloading model weights.
- [x] The cache directory and local-files-only behavior remain available for
      reproducible or offline use, while `from_huggingface_cache` remains
      compatible.
- [x] Installed-wheel tests cover aliases, canonical IDs, cache hits, filtered downloads, offline misses, and actionable unknown-model errors.

## Worklog

- [2026-08-27 09:00] [codex:e2e2e995] — 2026-08-27 implementation decision: construction will use the new public Python Processor facade owned by sibling ticket 6eea, avoiding concurrent edits to native `lib.rs` also touched by 70c3. Added isolated `_pretrained.py`: it resolves only exact public names `Qwen3`, `Qwen3.5`, `Qwen/Qwen3-VL-8B-Instruct`, and `Qwen/Qwen3.5-9B` against native bundled profile metadata, calls `huggingface_hub.snapshot_download` at the compiled immutable revision, and supplies the exact eight artifact filenames from each compatibility profile as `allow_patterns` so model weights cannot match. Added the pinned `huggingface-hub==1.26.0` runtime dependency and refreshed `uv.lock` offline. Awaiting the 6eea facade scaffold to add the thin public classmethod and installed-wheel tests.
- [2026-08-27 09:05] [codex:e2e2e995] — Verification update: targeted Ruff format/check and `git diff --check` pass. `crates/qwen-mm-python/tests/pretrained.py` passes against the editable package. A fresh-wheel `make python-binding-test` built and installed the wheel with `huggingface-hub==1.26.0`; the new installed-wheel pretrained suite passed, as did docs examples. The shared command then failed in sibling ticket 6eea's still-active `media_sources.py` cache-key assertion, which has been reported to that ticket owner; no 5fb8 construction assertion failed.
- [2026-08-27 09:11] [codex:e2e2e995] — Implementation complete for root review. Files owned by 5fb8: `crates/qwen-mm-python/python/qwen_mm/_pretrained.py` (canonical identity resolution plus pinned/filtered standard-HF download), the `from_pretrained` import/classmethod hunk in the shared `_processor.py` facade, `crates/qwen-mm-python/tests/pretrained.py`, the pretrained-suite line in `scripts/test-python-binding.sh`, and the `huggingface-hub==1.26.0` additions to `pyproject.toml`/`uv.lock`. `Processor.from_pretrained` accepts exactly `Qwen3`, `Qwen3.5`, `Qwen/Qwen3-VL-8B-Instruct`, and `Qwen/Qwen3.5-9B`; uses the immutable compiled revision; passes exactly eight manifest processor/tokenizer filenames per profile to `allow_patterns`; supports `cache_dir`, `local_files_only`, limits, and thread budget; and leaves `from_huggingface_cache` behavior intact. Final verification: `.venv/bin/ruff check` on construction files passed; editable `tests/pretrained.py` passed; `git diff --check` passed; `make check` passed (Ruff, Clippy, 124 Rust tests with 97 passed/27 fixture-dependent ignored, doctests, 133 reference tests, golden/conformance validation, and fresh wheel smoke); final `make python-binding-test` passed all fresh installed-wheel suites (`pretrained`, docs, media sources, usability, binding) and installed the declared Hugging Face dependency. No known 5fb8 concerns remain; root should review the shared facade integration before acceptance.
- [2026-08-27 09:15] [codex:ad56e22c] — Root review accepted the construction slice and shared-facade integration. The exact Qwen3/Qwen3.5 names and canonical model IDs resolve from bundled profile metadata, snapshot_download is pinned to each compiled revision with exact processor artifact allow-lists, and cache-only compatibility is preserved. Root fresh-wheel make python-binding-test and repository-wide make check both passed.

## Lifecycle

- 2026-08-27T13:15:30Z [codex:ad56e22ce3deade5] — closed (completed)
