---
id: "769d"
status: in_progress
deps: []
links: []
created: 2026-09-23T15:11:59Z
type: task
priority: 2
assignee: codex:09044ae762a8387c
parent: 57e6
---
# Open-source qwen-mm on GitHub with CI and trusted publishing

Publish the repository publicly under jbohnslav/qwen-mm with Apache-2.0 licensing, working native CI, release artifacts, and GitHub Actions publishing to PyPI. User explicitly authorized opening and publishing the project on 2026-09-23.

## Acceptance Criteria

- [x] Public GitHub repository exists with the reviewed source and Apache-2.0 metadata.
- [ ] CI validates Linux x86_64 and macOS ARM64 builds and installed examples.
- [ ] Release workflow builds verified distributions and publishes through a scoped PyPI trusted publisher.
- [ ] A GitHub release and installable packages are verified, or any external account blocker is explicit.

## Current plan

Retain the existing Apache-2.0 license. Include dependency attribution in distributions,
set project URLs, pin GitHub Actions, and connect the existing native release verification
to release artifact upload and trusted publication. GitHub owner: jbohnslav. First version:
0.1.0. Publish the core package first. Defer the optional vLLM package until its
security upgrade tracked in b270; do not publish the vulnerable 0.23.0 dependency pin.

The user has a PyPI account; Chrome is at its login page awaiting sign-in. No PyPI token
is requested or stored. The user's explicit 2026-09-23 instruction authorizes making
the repository public and publishing; the prior separate release-approval hold is superseded.

## Implementation and verification

The public repository https://github.com/jbohnslav/qwen-mm exists and `origin` is
configured; source was pushed at 16080c5 and master is the default branch. Existing Apache-2.0 licensing is retained.
Draft changes add project URLs, NOTICE and generated third-party license texts,
contribution/security documentation, pinned Actions, native candidate verification,
release artifact assembly with digest/source checks, GitHub release creation, PyPI
OIDC publication, and installed-package/digest verification. The native verification
workflow is reusable by the publishing workflow. Core wheels publish from `release.yml` using the `pypi` environment; optional
vLLM wheels are excluded pending b270.

Verified so far: pre-publication history secret scan (findings explained below),
`uv lock --check`, repository Ruff checks, actionlint 1.7.12, all script unit tests,
and a production macOS ARM64 wheel build whose embedded license texts and metadata
pass the release inspector. Artifact assembly tests reject tampering, stale commits,
missing platforms, failed steps, and unrelated vLLM evidence. GitHub pypi environment
and private vulnerability reporting are configured. These are not evidence of
passing hosted CI or successful publication; hosted CI exposed missing tokenizer assets in Rust jobs; setup is now fixed
locally and awaits a new hosted run.

## Review automation

The user also requested matching Codex and Claude review integrations from their
other repositories. Kingdom uses Claude code-review@claude-code-plugins only on
pull_request opened, and trusted-human @claude mentions for additional work.
Mirror those triggers here to avoid review loops. Kingdom has a repository-scoped
CLAUDE_CODE_OAUTH_TOKEN secret; GitHub does not allow reading its value back.
Codex review is configured through its GitHub app/repository settings, not a local
workflow found in the inspected repositories. Review setup is not yet complete.

## Blockers and next steps

- PyPI account exists, but browser sign-in is pending. After sign-in register pending
  trusted publisher for qwen-mm, owner jbohnslav, repository qwen-mm,
  workflow release.yml, environment pypi.
- Local assembly tests, workflow validation, packaged license checks, and release
  documentation are complete. Hosted verification is next.
- Source and owning tickets committed and pushed; default branch master and PyPI
  environment configured. Unrelated ticket 9516 remains untouched.
- Run hosted native CI and release-candidate verification; resolve failures before
  tagging v0.1.0. Then verify GitHub assets, PyPI publication, and fresh installation.

## Worklog

- 2026-09-23: Checked 123 Git commits with gitleaks 8.30.1 before public upload.
  All 191 findings were inspected: 190 SHA-256 evidence values and one artifact
  filename constant; no credential finding remained. Existing Apache-2.0 license,
  pinned release runners, and native test workflows found. Began public repository
  and trusted-publishing integration.

- 2026-09-23 checkpoint: Public repository created; publishing and attribution changes
  drafted locally. No source push, hosted verification, release tag, or PyPI upload yet.

- 2026-09-23: Pushed 16080c5 publicly. 113 script tests, repository Ruff,
  actionlint, production wheel build and embedded-license inspection passed.
  First hosted CI: https://github.com/jbohnslav/qwen-mm/actions/runs/35881656006.

- 2026-09-23: GitHub dependency scan reported advisories against optional
  vllm==0.23.0. Created backlog b270. Asked user about expanding scope; default
  is core-only publication, with plugin excluded and explicitly documented.

- 2026-09-23: User asked about CI failure and review integrations. Latest master
  CI 35882102080 and candidate 35882101804 were still running, with wheel smoke
  passed and no failing master job yet. Earlier superseded runs were cancelled.

- 2026-09-23: First actual master CI failure: nine Linux Rust processor tests
  lacked pinned tokenizer.json assets. Added the same pinned Processor download
  setup used by release verification to both native Rust CI jobs. Claude workflows
  copied from Kingdom with pinned Actions; OAuth secret and Codex repository
  setting still need configuration.
