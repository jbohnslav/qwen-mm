---
id: "4d7f"
status: in_progress
deps: []
links: []
created: 2026-08-13T05:11:45Z
type: task
priority: 2
assignee: hand
parent: 9516
---
# Select and integrate the fastest suitable Rust image resizer

D4 diagnostics show the scalar source-faithful Pillow resize port makes
single-image preprocessing roughly 45-49% slower than the official Pillow
baseline. Replace it with the fastest maintained open-source Rust resizer that
preserves structural processor correctness and beats the official end-to-end
baseline in every applicable image case. Pillow pixel identity is no longer a
selection goal.

## Scope

- Benchmark the official Pillow 12.3.0 resize boundary separately from Python,
  decode, normalize, and patchify overhead.
- Evaluate maintained open-source Rust-native candidates, including at least
  `fast_image_resize` and `pic-scale`, with `image`/`resize` retained as useful
  references. Prefer runtime-dispatched SIMD and caller-owned RGB8 buffers
  without adding a heavyweight system dependency.
- Compare relevant filters as performance/quality choices; do not require the
  winner to reproduce Pillow's bicubic weights, rounding, or adversarial pixels.
- Freeze a candidate-independent neural-image fidelity envelope before selecting
  the winner. Preserve exact geometry, keys, dtypes, shapes, strides, grids,
  ordering, limits, stable errors, no-op/constant/primary/channel invariants,
  video behavior, determinism, and downstream operations from candidate RGB;
  report per-occurrence/channel maximum error, RMSE, bias, p50/p90/p99, SSIM,
  MAE, PSNR, and representative natural-image diagnostics.
- Record dependency, wheel/distribution, platform, memory, and thread-budget
  tradeoffs as well as latency.

## Non-goals

Do not add an OpenCV/libvips C/C++ wrapper or another heavyweight runtime merely
to obtain a resizer. Do not spend this ticket hand-writing another complete
resize implementation before measuring maintained Rust libraries. Do not trade
away structural correctness, robust input/error behavior, or obvious natural-
image fidelity. Adversarial pixel identity to Pillow is explicitly not a gate.

## Acceptance Criteria

- [ ] The report distinguishes the current still-image path—Pillow bicubic in
      `qwen_vl_utils`, followed by Transformers with `do_resize=False`—from the
      separate TorchVision video path.
- [ ] At least two maintained optimized Rust implementations and the current
      scalar port are measured under identical inputs, output dimensions, warm
      state, and thread budgets on ARM and x86 where available.
- [x] A candidate-independent natural-image numerical fidelity envelope is
      documented and frozen before performance selection.
- [ ] The winner passes the frozen v2 quality gates and exact invariants while
      all structural processor outputs, downstream-from-candidate-RGB
      operations, video behavior, determinism, and errors remain exact.
- [ ] The selected option beats the official end-to-end baseline in every
      applicable D4 image case and both comparable thread regimes, profiles, and
      controlled architectures; the existing 2x headline batch gates remain.
- [ ] The winner does not regress D4 memory, parallel-efficiency, packaging, or
      one-total-thread-budget requirements.
- [ ] The revised compatibility/conformance corpus, full repository checks, and
      controlled D4 certification pass for the implementation change.

## Worklog

### 2026-08-13 — ticket created from D4 resize review

The current still-image implementation is a hand-written, scalar,
source-faithful port of Pillow 12.3.0's separable fixed-point resampler. The
top-level resize-stage contract permits a per-channel byte difference of one;
the selected port happens to be exact on the 17-case frozen corpus, but
universal bit-exactness was not the stated requirement. The only optimized Rust
candidate previously recorded was `fast_image_resize` 6.1.0 Catmull-Rom. It was
non-identical in 12 of 17 image cases, exceeded the then-stated one-byte bound
in 6 of 17, and differed by up to 21 byte levels. OpenCV and other mature native
backends were not evaluated. Related active certification ticket: `050f`.

### 2026-08-13 — King prioritizes neural preprocessing speed over Pillow identity

The King explicitly authorized relaxing image-resize numeric exactness. The
product requirement is now to select the fastest suitable open-source Rust
resizer and beat the official baseline in every applicable image benchmark
case. Exact geometry, public structure, metadata, types, ordering, resource
limits, and error semantics remain required; adversarial per-pixel equality to
Pillow does not. Heavy OpenCV-style wrappers and another speculative hand-rolled
kernel are disfavored. D4 and the Phase D epic now depend on this ticket.

### 2026-08-13 — v2 numerical and performance contract frozen

Added `docs/image-resize-contract-v2.md` before inspecting any `pic-scale`
result. It limits the relaxation to official Pillow versus candidate
still-image resized RGB8 and consequent tensors. Geometry, structure, no-op,
constant/primary/channel invariants, video, errors, determinism, and downstream
operations from candidate RGB remain exact. Each occurrence/channel must meet
max absolute error <=32, RMSE <=5, p99 absolute error <=16, absolute signed
mean bias <=2, and the specified 11x11 Gaussian SSIM >=0.98. A new fixed-seed
holdout is required because the existing corpus had already been used to
inspect `fast_image_resize`. D4 now requires a lower 95% speedup confidence
bound strictly above 1.0 for every shipping image coordinate while retaining
the 2x headline batch and <=5% text guards. Authenticated v1 artifacts retain
their historical meanings.

### 2026-08-13 — maintained-Rust bake-off checkpoint on M4 ARM64

Added an informational, reproducible 17-case still-image bake-off with
`pic-scale=0.7.11` exact-pinned as a dev dependency alongside the existing
`fast_image_resize=6.1.0` candidate. The runner authenticates the v1 manifest
and blobs, reads its one-byte tolerance instead of hard-coding exactness,
separates exact from legacy-contract failures, and reports both reusable-kernel
time and integration time including validation, padded-row packing, plan/weight
setup, allocation, resize, and packed `Vec<u8>` output. It does not alter the
production resizer or video path.

On this local M4 ARM64 diagnostic, every tested candidate beat the scalar port
on all 12 cases that actually resize. Integration geometric-mean speedups were
`6.06x` for pic-scale Bicubic, `5.89x` for pic-scale Catmull-Rom, and `2.82x`
for FIR Catmull-Rom. The pic-scale integration uses its native strided source
view rather than charging an unnecessary packed copy. Five no-op cases favor
the existing direct copy and must keep that dispatch. Bilinear and Area were
faster but showed clearly excessive
legacy-corpus error. Across aggregate legacy diagnostics, pic-scale Bicubic and
Catmull-Rom each reached max error 24, RMSE 3.80, p99 15, and absolute bias
1.01; FIR Catmull-Rom reached max 21, RMSE 3.62, p99 16, and absolute bias
0.073. These numbers are shortlist evidence only: they are one unpinned ARM
host, lack per-channel SSIM and the blinded v2 holdout, and do not prove the
official end-to-end or controlled ARM/x86 performance gates.

`zenresize=0.3.1` was trialed and removed: its available license is AGPL-3.0
or commercial and its Catmull-Rom sRGB path was slower than the scalar port in
all 17 local cases. No zenresize dependency remains. The next quality decision
must use the already-frozen v2 per-channel gates and fixed-seed holdout before
production integration.

### 2026-08-13 — candidate-blind v2 holdout frozen

Committed the seed-`20260813` v2 holdout before running any candidate against
it. The 45 cases include exact no-op/constants/primaries/channel invariants;
ramps, impulses, edges, checkerboards, one-pixel stripes, independent and
alternating channels, deterministic noise, one-axis and extreme-anisotropic
scales, and factor/min/max boundaries. Four fixed crops from a committed NASA
Earth Observatory Blue Marble asset provide genuine natural-image coverage;
the original URL, exact SHA-256, NASA credit, and usage-policy URL are recorded.
Existing project-generated JPEG crops are labeled procedural rather than
natural. The manifest authenticates its generator, locked Pillow oracle,
contract, every source/Pillow slice, the source asset, and all provenance.

The comparator applies every v2 gate independently to each occurrence and RGB
channel, retains all passing diagnostics, implements the specified reflect-101
SSIM, and checks cross-case channel permutation plus per-case exact invariants.
Corpus verification, Pillow self-comparison, three direct comparator tests,
Ruff, and byte-identical regeneration pass. This is now immutable selection
evidence; neither the contract, generator, source assets, nor Pillow outputs may
change after candidate evaluation begins.

### 2026-08-13 — frozen-holdout candidate results

Evaluated only after holdout commit `6d8d48b`. The scalar port and FIR 6.1.0
Catmull-Rom pass all 45 v2 cases, every per-channel numerical/SSIM gate, and all
exact invariants. Default-speed pic-scale 0.7.11 Bicubic and Catmull-Rom pass
every numerical/SSIM gate but fail the exact constant-RGB invariant on one
extreme-anisotropic midgray case: most output bytes are 129 rather than source
and Pillow value 128. That is a real contract failure and was not waived.

Both pic-scale filters with public `WorkloadStrategy::PreferQuality` fully pass:
zero case, channel-gate, exact-invariant, or channel-permutation failures. Their
worst per-channel diagnostics remain inside the predeclared envelope: max error
30 (`noise-up` R), RMSE 3.59623 (`factor-below` R), p99 14
(`factor-below` R), absolute bias 0.70833 (`channel-permutation-bgr` G), and
minimum SSIM 0.993704 (`nyquist-horizontal-stripes` B). On ARM this strategy
selects pic-scale's higher-precision i32 RGB8 path instead of its lower-precision
RDM path. Final backend/filter selection still depends on the setup-inclusive
performance comparison and controlled end-to-end gates.

### 2026-08-13 — pic-scale Bicubic PreferQuality selected for integration

The optimized M4 setup-inclusive comparison across the 12 actual-resize cases
measured geometric-mean speedups versus the scalar port of `5.50x` for
pic-scale Bicubic PreferQuality, `5.36x` for pic-scale Catmull-Rom
PreferQuality, and `2.81x` for FIR Catmull-Rom; every passing candidate won all
12 cases. PreferQuality costs about 8% versus pic-scale's failing default-speed
Bicubic mode but remains almost twice as fast as FIR. The two passing pic-scale
filters differ in only 82 of 2,914,752 v2 output bytes, with maximum difference
2 and no meaningful quality winner. Bicubic PreferQuality has the slightly
lower aggregate MAE and higher measured integration speed, so it is selected.

Production integration will retain qwen-mm's exact validated no-op row-copy,
use pic-scale only for actual resizes, pass native strided RGB8 without a
packing copy, force `ThreadingPolicy::Single`, and add no persistent plan or
scratch cache. The current setup-inclusive margin does not justify a cache's
memory-accounting and concurrency complexity. Controlled official end-to-end
ARM/x86 certification remains a later acceptance gate, not something claimed
by this local stage result.

### 2026-08-13 — selected production backend integrated and independently reviewed

Moved exact-pinned `pic-scale=0.7.11` into the core production dependencies and
replaced the still-image actual-resize dispatch with Bicubic,
`PreferQuality`, and `ThreadingPolicy::Single`. The public path borrows validated
positive-stride RGB8 directly, allocates destination and exact requested scratch
fallibly, and retains the exact checked no-op row-copy. The source-faithful
scalar Pillow port is test-only historical evidence; video is unchanged.

The complete production output on the frozen 45-case v2 holdout passes every
quality gate and exact invariant. Its 2,914,752-byte SHA-256 is
`f4ea0c36e3a65000576e38b772080a69dbe3bd664e428137aa99846a96d32e6a`,
byte-identical to the selected direct pic-scale configuration. The diagnostic
now asserts that equality, and a unit regression locks the extreme
`31x257 -> 256x32` constant-midgray case that rejects `PreferSpeed`.

Independent review confirmed the strided last-row layout, single-thread policy,
absence of the scalar kernel from normal builds, and destination/scratch
observation cleanup. It also identified a dependency limitation now disclosed
in `docs/resize-selection-v2.md`: pic-scale does not expose fallible allocation
for internal per-call plan weights. Qwen-mm bounds those weights through checked
geometry/resource caps, but allocator exhaustion inside plan construction cannot
be translated to `resource_limit`; qwen-owned destination and scratch remain
fallible. Cross-candidate steady-kernel speedup summaries were removed because
the public production timing includes per-call setup while direct candidates
reuse it; setup-inclusive integration timing remains the comparable evidence.

`cargo fmt`, core all-target Clippy with warnings denied, and core all-target
tests pass (`97 passed`, `27` model-snapshot-dependent ignored, plus example
tests). Full workspace Python/binding checks await the versioned benchmark and
Phase C resize-v2 evidence layer in the same ticket.
