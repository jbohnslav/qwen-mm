---
id: "72e4"
status: closed
deps: [431a, 9580]
links: []
created: 2026-08-02T12:28:51Z
type: task
priority: 2
closed_at: 2026-08-02T16:04:13Z
assignee: hand
parent: 9e1d
---
# C3: Pass the full image conformance gate

## Goal

Validate the installed Python-facing Rust batch path against the complete A3
text/image compatibility corpus for both frozen model profiles and publish the
first reproducible conformance report. Closing this ticket is the correctness
gate that permits Phase D profiling and performance work to begin.

## Scope

- Run the C2 Python batch API against every declared committed and live
  differential text/image case from A3 for both pinned profiles.
- Compare rendered/expanded prompts, conditional key presence, dtypes, shapes,
  strides, IDs, masks, modality IDs, grids, offsets/ranges, placeholder counts,
  occurrence order, prepared RGB, final pixels, and stable error categories
  under the frozen exact/numeric comparison policy.
- Exercise heterogeneous multi-request batches and batch permutations, not only
  the Phase B single-request cases, including boundary dimensions, codecs/color
  modes, raw strided RGB, malformed media/messages, interleaving, repetition,
  and resource/overflow failures.
- Publish a machine-readable result and readable summary with profile, source,
  artifact, package, platform, command, and comparator provenance sufficient to
  reproduce the gate from a clean checkout.
- Keep correctness evidence separate from diagnostic timing. Fail the gate on
  missing outputs, widened tolerances, skipped declared cases, or fallback to
  the official implementation inside the candidate path.

## Non-goals

Do not claim or certify speed, add parallelism or fusion, expand the frozen
compatibility envelope, add video parity, or implement vLLM production
integration.

## Acceptance Criteria

- [x] Qwen3-VL-8B-Instruct and Qwen3.5-9B pass the entire declared A3
      text/image corpus through the installed C2 Python API with no undeclared
      skips or candidate-path Python/Hugging Face/Qwen VL Utils fallback.
- [x] All discrete metadata, output keys, dtypes, shapes, strides, padding,
      ordering, and stable valid/invalid error categories match exactly.
- [x] Prepared RGB and `pixel_values` satisfy the immutable per-stage and
      end-to-end tolerances, with full mismatch localization and ULP/error
      diagnostics retained on failure.
- [x] Heterogeneous batch and permutation cases prove request, occurrence,
      token, patch, and grid order is deterministic and independent of grouping.
- [x] A versioned machine-readable conformance report and human-readable summary
      are committed with complete reproducibility and provenance metadata.
- [x] The report contains no performance claim, and the benchmark tooling cannot
      mark an image speed result releasable unless this gate is current and
      passing.
- [x] Report validation, a clean reproducibility run, wheel build/install smoke,
      and the full repository correctness suite pass.

## Worklog

### 2026-08-02 — full gate and reproducibility passed

Committed the corrected gate source at `0364b5a` and ran
`make phase-c-release-check` from that clean gate-input revision. The full
repository checks, Phase B gate, wheel smoke, installed Python binding suite,
and Phase C matrix pass. Both profiles executed all 290 declared candidate
cases with zero skips and zero failures; all 16 helper fixtures also passed
under their explicit non-candidate classification.

Published `reference/phase-c/v1/report.json` and `summary.md`. The report records
source fingerprint
`e8f5ee5489530b0744a93fbf5a16c3ec5c560e3d91f613216300d17a02881cbf`,
clean revision `0364b5acc1f6f86b993432ca8cf6557bfe049463`, NumPy `2.4.6`,
and the production package/native artifact identities. Standalone report
validation passes.

An independent second clean run to separate evidence paths also passed 290/290.
Its case inventory, results, source inputs/fingerprint, assets, comparator,
boundary map, exclusions, fixture evidence, runtime identity, and Git/platform
provenance match the committed report; the readable summaries are byte-exact.
Only run time and the wheel ZIP container digest are run-specific, while the
embedded package and native artifact hashes match exactly.

### 2026-08-02 — first full gate localized and corrected

The first clean release-check completed all 290 candidate cases with no skips
and localized 12 harness comparison failures: two `image24` sidecar checks used
expanded rather than rendered code-point spans; edge-limit probing observed the
first checked dimension rather than the longest edge; and the output-byte axis
used the obsolete shorthand instead of the public
`materialized_output_bytes_per_batch` field. The underlying installed outputs
and committed golden remained correct.

Corrected the exact sidecar normalization and public resource-boundary
construction, added regressions for all three causes, and made the synthetic
benchmark smoke test independent of whether Phase C evidence already exists.
All 24 focused Phase C tests and all 66 reference tests pass. A new clean source
commit and full gate rerun remain pending.

### 2026-08-02 — implementation frozen for the clean gate

Implemented the installed-production-wheel driver, strict v1 JSON Schema,
290-case canonical inventory, exact comparator/currentness validation,
wheel-to-runtime artifact binding, readable summary, and fail-closed benchmark
release prerequisite. The public NumPy boundary now also accepts positive
row-padded packed-RGB arrays while rejecting negative, overlapping, transposed,
and channel-sliced layouts.

Independent corpus, schema, and release-guard reviews drove tamper coverage for
dropped/duplicate cases, widened tolerances, stale sources/assets/Git revisions,
performance language, swapped wheels, changed adapters, and image-to-text
benchmark relabeling. Before the clean full gate, 21 focused Phase C tests, 22
benchmark tests, all 60 reference tests, Ruff, strict Clippy, PyO3/NumPy tests,
and the installed-wheel Python binding suite pass. The final report and full
repository release check remain pending, so this ticket stays in progress.

### 2026-08-02 — implementation started after C2

Started after C2 closed and committed at `ee1a932`. Per the King's direction,
the Lord will use one Codex implementation sub-agent plus independent read-only
corpus and release-gate reviewers; no Kingdom peasant will be started. The gate
will exercise the installed wheel rather than an in-tree Python import or an
internal Rust shortcut, retain complete comparator diagnostics, and commit both
machine-readable and readable versioned evidence without making a speed claim.

### 2026-08-02 — ticket created

Scoped from ROADMAP C3. The ticket depends on the closed A3 corpus/comparator and
on C2 so the conformance gate measures the actual installed Python-facing batch
release rather than an internal Rust-only shortcut.
