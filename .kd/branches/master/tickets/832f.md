---
id: "832f"
status: closed
deps: [9754, 8eb9]
links: []
created: 2026-08-01T00:29:44Z
type: task
priority: 2
closed_at: 2026-08-01T01:38:16Z
assignee: hand
parent: fa8c
---
# B2: Implement tokenizer and chat rendering

## Goal

Reproduce the declared Qwen3-VL and Qwen3.5 text surfaces in native Rust so
rendered prompt bytes and all padded text arrays exactly match the pinned
official composed path.

## Scope

- Load and validate each local `tokenizer.json`, special-token configuration,
  and template asset through B1's immutable profile.
- Use the Rust `tokenizers` crate for tokenization and implement only the chat
  surface frozen in A1: roles/content, tools and tool calls/results, generation
  prompts, vision IDs, padding, and profile-specific options.
- Preserve the Qwen3.5 reasoning/thinking and whitespace behavior and reject
  Qwen3.5-only options on the Qwen3-VL profile.
- Accept deterministic visual replacement information so text processing can
  emit exact expanded prompts, `input_ids`, `attention_mask`, and
  `mm_token_type_ids` without performing image processing itself.

## Non-goals

Do not add arbitrary Jinja/template overrides, truncation, documents, audio,
network asset loading, a Python callback, or permissive behavior outside the
accepted compatibility surface.

## Acceptance Criteria

- [x] Rendered and visual-expanded prompts match the A2 UTF-8 bytes exactly for
      both profiles, including whitespace, Unicode, special tokens, tool JSON,
      generation prompts, vision IDs, and Qwen3.5 thinking/reasoning cases.
- [x] Token IDs and special-token placement match exactly for committed goldens
      and the complete A3 chat corpus on both profiles.
- [x] Padding uses the profile-specific pad token and produces exact `int64`
      `attention_mask` and `mm_token_type_ids` values, shapes, order, and
      C-contiguous row strides.
- [x] Visual placeholder counts and replacement spans are checked against the
      supplied occurrence/grid plan; missing, extra, literal, interleaved, and
      repeated visual tokens follow the frozen valid/error behavior.
- [x] Unsupported roles/options, malformed tool content, profile-option
      mismatches, and configured token-limit failures return the correct stable
      B1 category and deterministic precedence.
- [x] Tokenizer/template assets are loaded locally and hash-validated; tests
      prove no Python, Hugging Face, Qwen VL Utils, or network fallback exists.
- [x] The added dependency versions are exact-pinned and justified; core unit,
      conformance-smoke, format, Clippy, and doc tests pass.

## Worklog

### 2026-08-01 — ticket created

Scoped from ROADMAP B2 with A2 prompt/output goldens and B1 profile/types as
hard prerequisites.

### 2026-08-01 — implementation started

Started after B1 closed. A collaboration subagent owns tokenizer/chat code,
tests, and any exact-pinned text dependencies; the Lord owns ticket state,
review, acceptance, and commit. B2 is running in parallel with independent B3
geometry work.

### 2026-08-01 — native text path implemented

- Added exact-pinned Rust `tokenizers` 0.22.2 with only the Oniguruma backend;
  no Hub or network feature is enabled. `TextProcessor` accepts an explicit
  local snapshot and validates the profile plus tokenizer, tokenizer-config,
  and profile-specific template SHA-256 values before parsing.
- Implemented the frozen Qwen3-VL and Qwen3.5 renderers directly in Rust for
  roles/content, tools and both tool-call shapes, consecutive tool responses,
  generation prompts, vision IDs, Qwen3.5 reasoning extraction/content, and
  thinking defaults/toggles. Arbitrary Jinja is not exposed.
- Added checked ordered image/video expansion plans, timestamp syntax,
  placeholder/grid validation, exact code-point/token spans, literal-token
  rejection, and right-padded C-contiguous `i64` IDs, masks, and modality IDs.
- Added token/output capacity preflight plus documentation of the small offline
  core dependency set.

### 2026-08-01 — Lord review and corpus correction

The first review found that representative cache-backed probes could skip in a
clean checkout and did not satisfy the ticket's literal complete-A3-corpus
criterion. B2 remained open. The correction added a deterministic 15-case
`reference/conformance/v1/chat.json` corpus covering both profiles, every A3
chat recipe dimension, interleaved/repeated visuals, padding, and stable error
cases; an official pinned generator; and always-on Rust render/expand/error
checks plus a separate local-snapshot exact-array tier.

The Lord also rejected the generator's initial stale `reference/.venv` command.
`make chat-conformance` now uses the canonical root locked uv workspace. The
command is documented and embedded/asserted in the fixture.

### 2026-08-01 — final verification

- Independently ran `make chat-conformance`; the regenerated 15-case file was
  byte-identical at SHA-256
  `302274664d96c63c5b92788aa053b92372a63dcfdead30654ec03a5f34fec5da`.
- The explicit local-oracle tier passed all 8 ignored tests against both exact
  cached snapshots, including complete fixture IDs, masks, and modality IDs.
- `make rust-check` passed formatting, strict workspace Clippy, 42 normal core
  tests with 8 clearly marked local-asset tests skipped, all workspace/doc tests,
  and the locked offline core build.
- Independent `make check` passed Ruff, the full Rust suite, 30 reference tests,
  fixture/golden/corpus validation, conformance smoke, and wheel smoke.
- `git diff --check` passed. No Python/Hugging Face/Qwen VL Utils callback or
  network fallback exists in the Rust runtime path.

All seven acceptance criteria are satisfied.
