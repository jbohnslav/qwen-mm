---
id: "e182"
status: in_progress
deps: [8b14, 72e4, ccbc]
links: []
created: 2026-08-02T22:36:40Z
type: task
priority: 2
assignee: hand
parent: 9516
---
# D1: Add native observability and publish profiles

## Goal

Make the complete structured-input-to-materialized-NumPy operation observable,
then use the existing paired benchmark workloads to publish a ranked,
evidence-backed bottleneck list on ARM and x86. This ticket establishes the
measurement basis for D2 and D3; it does not optimize based on assumptions.

## Scope

- Add request- and media-scoped spans for planning, decode/color conversion,
  resize, normalization, patchification/layout, chat rendering/tokenization,
  destination execution, and the Python/NumPy binding boundary.
- Count native and Python calls at the stage boundaries so profiles expose
  accidental callbacks, duplicate processing, or unexpected fallback work.
- Count allocations, allocated/copied bytes, buffer lifetimes, peak transient
  live bytes, and retained final-output bytes without conflating the 432 MiB
  materialized result with scratch memory.
- Produce whole-operation flamegraphs or equivalent sampled profiles using the
  A5 workload contract, including `jpeg24_one_request`, `ragged24`,
  `jpeg1_offgrid`, both text cases, and `rgb24` in representative one-thread
  and production-thread configurations.
- Publish separate ARM and Linux x86 evidence with exact source revision,
  build mode, profile/model, workload, thread budget, profiler, commands, and
  host provenance, followed by a ranked bottleneck and allocation/copy list.
- Keep observability deterministic, testable, and either disabled or bounded
  in release timing so instrumentation does not silently become the workload.

## Non-goals

Do not add parallel execution, fusion, arenas, new output modes, video support,
or a certified speed claim. Candidate optimizations discovered here belong to
D2 or D3 and must cite the published evidence.

## Acceptance Criteria

- [ ] Every declared native and binding stage emits request/media-correlated
      spans with stable names, ordering, byte/shape counts, and success/error
      outcomes; tests cover text-only, encoded-image, raw-RGB, repeated-image,
      heterogeneous-batch, and failing requests.
- [ ] Allocation instrumentation distinguishes retained outputs from transient
      buffers and reports allocation count, copy count/bytes, live bytes, and
      peak transient live bytes; focused tests reconcile counters against known
      small workloads.
- [ ] Python-call counters prove the candidate batch path performs one public
      Python call and no callback per visual, and expose any unexpected
      candidate-path Hugging Face, Qwen VL Utils, Pillow, or TorchVision work.
- [ ] Whole-operation profiles are captured for the required core cases on a
      native ARM host and a native Linux x86 host under explicit equal thread
      budgets and current passing C3 conformance evidence.
- [ ] A versioned machine-readable profile summary plus readable report ranks
      time, allocation, copy, and buffer-lifetime bottlenecks separately for
      ARM and x86 and identifies the evidence that will justify D2/D3 work.
- [ ] Observability-disabled results remain exactly conformant for both
      profiles, and enabled observability neither changes outputs/errors nor
      introduces unbounded per-request memory.
- [ ] The full repository correctness suite and profile-artifact validation
      pass from a clean checkout.

## Worklog

### 2026-08-02 — implementation started

Started after confirming A5 and C3 are closed and D1 is the only ready Phase D
ticket. Per the King's direction, a Codex collaboration sub-agent owns the
bounded implementation while the Lord retains architecture decisions,
independent review, acceptance verification, closure, and the scoped commit.
No Kingdom peasant worktree is being used. D2 and D3 will begin in parallel
only after D1 closes.

### 2026-08-02 — ticket created

Scoped from roadmap D1 with the closed A5 paired harness and C3 full image gate
as prerequisites. This is the explicit stop/go gate against speculative
optimization: D2 and D3 both depend on its published ARM/x86 evidence.

### 2026-08-02 — implementation audit started

The current C2 boundary is one `Processor.prepare_batch()` call that parses and
owns Python inputs, releases the GIL for serial `plan_batch` plus
`execute_plan_into`, and transfers the output `Vec` allocations into NumPy
without a copy. The core has no opt-in observability/report type, and the Python
package has no `qwen_mm.benchmark:create_adapter` factory despite that factory
being the A5 harness's documented candidate entrypoint.

D1 will therefore add a deterministic, bounded observed execution surface
covering stable native and binding stages plus owned-buffer/copy accounting,
and add reproducible capture/report validation around the existing A5 workload
contract. This checkout is native macOS ARM; no native Linux x86 host is
available here, so x86 evidence will be left as an explicit required capture
gap rather than inferred from the committed emulated conformance artifacts.

### 2026-08-02 — paused behind D0 Modal runway

At the King's direction, stopped both D1 collaboration sub-agents and returned
this ticket from `in_progress` to `open`. The partially implemented opt-in core
observability work remains uncommitted in the working tree as explicitly paused
WIP; it has not been reviewed, accepted, or claimed against any criterion. D1
now depends on D0 / `ccbc`, which will first prove the smallest viable native
Linux x86 Modal build and benchmark orchestration path.
- [2026-08-02 20:57] — Unblocked by closed D0 ccbc. Resumed the preserved observability WIP on master. Work is split across core instrumentation, Python/benchmark capture, and independent acceptance/test audit; the Hand will integrate, review, run ARM plus Modal x86 evidence, and commit only after all D1 criteria are verified.
- [2026-08-02 20:59] — Execution decision: Modal is a first-class host for real D1/D4 benchmark and profile evidence, not merely synthetic smoke. D0's synthetic/non-releasable labels applied only to its self-test artifact. D1 will package the 33 MiB hash-pinned local profile assets explicitly and capture the real qwen_mm candidate on native Modal x86 with the same cases and thread budgets as ARM.
- [2026-08-02 22:30] — Independent integration review found and assigned fixes before the clean capture commit. Real macOS `/usr/bin/sample` output exposed incorrect `+ !` call-tree parsing; pair validation did not bind every worker to the declared thread regime; failure spans ended before buffer cleanup; and malformed profile objects could escape as raw Python exceptions. The capture runner also could not yet launch honestly: omitted tracked `.kd` files made the uploaded checkout dirty, Modal materializes the 16 Hugging Face cache symlinks, Phase C scratch would inflate the return archive past 100 MiB, x86 evidence had no committed ingest path, and no one-command ARM lane existed. Repairs now require content-stable asset identity, bounded allowlisted archives, final `benchmarks/profile-evidence-v1` paths chosen at capture time, canonical post-ingest semantic validation, complete ARM/x86 merge/report workflow, and an early Linux native-sampler attach preflight. These are evidence-integrity blockers, not performance-target failures. D1 remains open until both reviewers' regressions pass and real ARM plus Modal x86 evidence is published.
- [2026-08-02 23:15] — Clean-capture implementation review passed. The final pipeline authenticates a commit-derived source tree on both hosts, builds and records one exact profiled wheel per host, binds fresh C3, paired real A5, bounded observations, and native sampled profiles to that runtime, and publishes only through a size-capped allowlisted archive. Post-ingest validation is architecture-portable and binds the durable Phase C path, report hash, gate fingerprint, runtime identity, workload, thread settings, and adapter wrapper without comparing a foreign wheel to the local extension. Dual-host ingest is transactional. Independent review found no remaining actionable issue after focused portable/tamper regressions; Ruff, runner tests, benchmark/profile tests, Rust/ignored asset tests, and binding tests are the pre-capture gates. Real ARM and Modal x86 capture remain required before any acceptance box is checked or the ticket is closed.
- [2026-08-02 23:38] — The first same-revision capture attempt proved the ARM build, fresh 290-case C3 gate, and installed-wheel validation, but Modal's Linux ptrace policy rejected py-spy's sibling `--pid` attachment before the x86 matrix. The ARM lane was deliberately stopped before publication because any archive from the superseded revision would be unusable. A direct-child Modal spike then proved pinned native `py-spy 0.4.1` can launch and symbolize a finite worker. The repair now makes py-spy launch the exact authenticated profile worker, records and binds that worker's runtime identity to the profiled build, reconciles py-spy's reported sample/error totals with the retained raw stacks, admits only whole-operation-marker stacks to rankings, and performs timeout/nonzero cleanup only for exact `/proc` command matches. The worker uses a two-second dispatch deadline and records completion of the final operation plus the unmarked correctness post-check. Independent focused/full tests pass, and final read-only review found no remaining P0/P1/P2 issue after bounding timeout cleanup before pipe draining. A new clean repair commit precedes both fresh host captures. This was an instrumentation-host compatibility failure, not a performance-target miss.
- [2026-08-03 00:15] — The next ARM capture passed build, fresh C3, and A5 but rejected the first real `/usr/bin/sample` profile because the validator expected demangled `qwen_mm_core::`/`qwen_mm_python::` names. The actual debug-enabled wheel emitted Rust-v0 symbols for `qwen_mm_core` and the binding cdylib crate `qwen_mm_native`, real `: | + !` call-tree guides, ASLR addresses, sampled offsets, and Rust source suffixes. The repaired parser consumes all 2,014 preserved call-graph lines, retains the honest 1,531-sample total with 702 canonical stacks and zero invalid child balances, recognizes all 96 core plus 47 binding frames, and removes address/offset/source fragmentation; an exact native coordinate then passed with 1,529 samples and 94.96% native share. Classifiers use demangled token boundaries or Rust-v0 crate-root encodings, with lookalike rejection tests, and are shared by capture, ranking, validation, and symbolization. Local and Modal runner errors now retain a bounded output tail even when scratch cleanup removes full logs. Full profile/runner/pre-commit suites and independent review pass with no actionable finding. The superseded Modal run was aborted before publication; both hosts still require fresh archives from the forthcoming repair commit. This was evidence-parser fidelity work, not a performance-target miss.
- [2026-08-03 00:28] — The following real ARM attempt used the new preserved output tail and exposed a 41 ns `direct child spans overlap` error at the first frozen observation coordinate. Diagnosis proved `SpanToken.started` captured one `Instant`, while exported `started_ns` used a later `self.started.elapsed()`; duration was measured from the earlier instant, shifting reported ends forward by begin-call overhead. The fix derives both values from the same instant and leaves the strict containment/overlap/exclusive-duration validators unchanged. A 512-adjacent-sibling regression catches the old skew deterministically. All 72 real frozen observation coordinates pass with the rebuilt profiled wheel, all 112 core tests (including the 22 asset-gated cases) pass, the full profile suite passes, and independent review found no issue. The paired Modal run was again aborted before publication because the source revision was superseded. This was observability timestamp integrity work, not a performance-target miss.
- [2026-08-03 01:15] — The first full direct-child Modal matrix passed its long build, C3, and A5 gates but py-spy 0.4.1 ended the first profile with `No child process (os error 10)` after the finite worker exited. Official source review identified a final child-wait race, so the worker now durably publishes its authenticated post-check result and remains alive until the controller verifies its PID plus exact `/proc` command and signals only py-spy. The controller requires sampler exit zero, exact raw/reported sample totals, zero errors, and that the child is no longer live; timeout, early-exit, PID-mismatch, and authentication-failure cleanup remains bounded to exact-command workers. The Modal lifecycle preflight passed with 90 samples and zero errors, and an exact profiled-release coordinate no longer reproduced `ECHILD`. Independent review found and closed missing-result and mismatched-result orphan paths; 17 focused process-control tests, 21 runner tests, the full 22-test profile suite, Ruff, formatting, and diff checks pass. A separate exact-workload run still failed the unchanged zero-error evidence gate with 165 native unwind/merge errors in 424 samples (`UNW_EINVAL`/`UNW_EBADREG`), while the trivial preflight remained clean. D1 therefore stays open while a Linux native-profiler alternative is evaluated; no noisy evidence is accepted. This is an evidence-profiler compatibility failure, not a performance-target miss.
