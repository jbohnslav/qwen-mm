---
id: "c3b2"
status: closed
deps: [832f]
links: []
created: 2026-08-01T03:41:51Z
type: task
priority: 2
closed_at: 2026-08-01T04:06:07Z
assignee: hand
parent: fa8c
---
# B2.1: Close A3 adversarial chat coverage gap

Extend the authenticated B2 chat oracle and Rust conformance tests to execute the A3 chat-text-adversarial and chat-batch-padding recipes that the closed B2 ticket claimed as complete.

## Acceptance Criteria

- [x] Both profiles match exact rendered/expanded bytes and token arrays for canonical/decomposed Unicode, empty text, and CRLF cases.
- [x] Both profiles match exact padded int64 IDs, masks, modality IDs, shapes, strides, row order, and deterministic permutations for lengths 0,1,31,32,33,1024.
- [x] The committed fixture remains deterministic and authenticated; make chat-conformance, local-snapshot exact tests, make check, format, Clippy, docs, and diff checks pass.

## Worklog

### 2026-08-01 — audit remediation complete

An independent Phase B epic audit found that B2's claimed complete A3 chat
coverage omitted the exact decomposed/composed Unicode pair, empty and CRLF
messages, and the declared padding/permutation lengths. The Lord created this
follow-up ticket and delegated its isolated fixture/test implementation to a
collaboration subagent; no Kingdom peasant worker was used.

The authenticated chat generator now reads the A3 recipe catalog directly and
executes both profiles for `naïve café 👩🏽‍💻`, `é versus é`, empty text, CRLF,
and the exact literal visual-token string (as `invalid_request`). It executes
identity plus two SHA-256-derived deterministic row orders for message lengths
`[0, 1, 31, 32, 33, 1024]`. Length is frozen as the count of U+0078 code points
in the sole pre-template message, which is also its UTF-8 byte length.

The fixture authenticates its generator, A3 catalog, compatibility manifest,
and canonical document. Two independent regenerations were byte-identical at
SHA-256 `d18383c64de0a3eab54aed9bbf594befb2d756f73db7925646da5cb2f2f2e5e8`.
The both-profile cache-backed exact-array tier, focused always-on coverage,
`make chat-conformance`, strict Clippy, formatting, docs, Ruff, and diff checks
passed. The Lord then ran `make check`: 71 normal Rust tests, 14 explicit
cache-gated tests skipped in the normal tier, reference/conformance tests,
corpus validation, doc tests, and wheel smoke all passed; all 14 cache-backed
core tests also passed separately.
