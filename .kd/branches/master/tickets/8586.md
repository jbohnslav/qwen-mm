---
id: "8586"
status: closed
deps: []
links: []
created: 2026-07-31T20:45:50Z
type: bug
priority: 2
closed_at: 2026-08-01T00:18:17Z
assignee: hand
---
# Repair compatibility-v1 lock provenance after workspace migration

A1 compatibility/v1.json records reference/uv.lock at SHA-256 30d82d0d..., but A4 moved the lock to root uv.lock and later workspace changes produced SHA-256 a674d993.... The pinned reference package versions are unchanged, but a clean checkout cannot verify the immutable profile's declared lock path/hash. Decide whether to restore an archived v1 lock snapshot or issue updated compatibility fingerprints without weakening offline reproducibility.

## Acceptance Criteria

- [x] The Phase A owner decides whether the archived reference lock must be
      restored or the recorded provenance drift is acceptable.

## Worklog

### 2026-07-31 — closed by Phase A owner decision

- The Phase A owner explicitly declined further lockfile work. The historical
  declared-lock mismatch is already visible in exported provenance, while root
  `uv.lock`, exact package versions, source hashes, model revisions/artifacts,
  profile fingerprints, and observed oracle outputs remain independently pinned.
- Decision: accept the archived-path mismatch as non-blocking historical metadata
  rather than restore a second lockfile or redefine compatibility-v1. No runtime,
  contract, fixture, tolerance, benchmark, or integration behavior changes.
- The temporary historical snapshot restored during audit was removed before
  closeout; root `uv.lock` remains the only active lockfile.
