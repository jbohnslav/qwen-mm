---
id: "f49c"
status: closed
deps: []
links: []
created: 2026-08-24T20:56:28Z
type: task
priority: 2
closed_at: 2026-08-26T20:48:42Z
resolution: completed
close_reason: "Bounded major-defect optimization pass complete; no qualifying production change, and further kernel-level profiling is intentionally out of scope for v0.1."
closed_context: codex:fd99c9f9755e8c3a
assignee: codex:fd99c9f9755e8c3a
parent: 9516
---
# Run the bounded multithreaded optimization pass for v0.1

## Goal

Make one profile-driven pass over qwen-mm's t8 scaling before v0.1, prioritizing
the larger x86 gap while retaining deterministic processor-owned bounded
parallelism. Improve user-visible t8 latency where low-risk evidence supports
it; the original `E_8 >=0.6` target remains published but is not the close
condition.

## Scope

- Profile t1/t8 scheduling, work partitioning, synchronization, allocator
  pressure, serial fractions, and resizer behavior on x86 first and ARM second.
- Apply only bounded, low-risk changes within the same eight-thread budget.
- Measure each change on both profiles and architectures and retain it only
  when t8 improves without harming t1, correctness, memory, or pool semantics.
- Publish the final absolute speed and internal efficiency honestly even if the
  former 60% target remains unmet.

## Non-goals

Do not relabel the old certification, add hidden threads, oversubscribe through
a nested pool, create architecture-dependent outputs/errors, or trade away t1
speed, conformance, or bounded memory. Avoid speculative scheduler rewrites
once the ranked low-risk opportunities are exhausted.

## Experiment TODO

Run these as four isolated experiments in order. For each experiment, capture
the exact source/diff identity, measure both profiles at t1 and t8 on x86 first
and ARM second, record wall time, CPU utilization, internal efficiency,
allocation/transient-memory evidence, and exact output signatures, then retain
or revert it before starting the next experiment.

### 1. Retained-output allocation and initialization

- [ ] Add current-path timing/utilization attribution around capacity discovery,
      destination reserve/initialization, and final destination writes without
      routing the production t8 benchmark through the serial observed path.
- [ ] Measure a same-pool, same-eight-worker implementation that initializes
      large destinations in bounded parallel chunks; keep t1 on the serial
      compatibility path if parallel setup would add overhead.
- [ ] If initialization is confirmed dominant and parallel zeroing leaves a
      material redundant-write cost, prototype an uninitialized-destination API
      separately and prove every element is initialized before success, no
      uninitialized storage is exposed on error, and destination validation and
      error precedence remain unchanged.
- [ ] Retain only a repeatable t8 win with no material t1, memory, correctness,
      allocation-census, or pool-semantics regression; otherwise document and
      revert both variants.

### 2. Pic-scale plan and scratch reuse

- [ ] Attribute pic-scale plan construction, destination allocation, scratch
      allocation/initialization, and resampling separately for current image24
      on both architectures.
- [ ] Measure batch-local immutable plan reuse keyed by the complete validated
      source/destination geometry and selected resizer configuration.
- [ ] Measure bounded per-worker scratch reuse as a separate variant so its
      effect is not conflated with plan reuse.
- [ ] Preserve `ThreadingPolicy::Single`, the single processor-owned pool,
      stable error order/categories, exact thread-count repeatability, and
      complete allocation/transient-memory accounting.
- [ ] Retain qualifying plan and/or scratch reuse independently; document and
      revert each non-qualifying variant.

### 3. Patchify kernel and work partitioning

- [ ] Capture per-stage core utilization and worker occupancy for uniform
      image24 and ragged24, separating final-output bandwidth from scheduler
      idle time and allocator stalls.
- [ ] Measure a pixel-row/output-weighted split for heterogeneous image batches;
      do not claim it addresses uniform image24 unless the profile demonstrates
      an effect there.
- [ ] Only if leaf granularity leaves workers idle, measure bounded intra-image
      patchify splitting into disjoint final-output slices without changing
      array order or floating-point operations.
- [ ] Do not retry the rejected normalization LUT. Preserve the retained
      temporal-plane reuse and exact output bytes across thread counts.
- [ ] Retain only independently repeatable improvements; document and revert
      scheduler or kernel variants that regress t1, memory, or correctness.

### 4. Cached profile and plan identity

- [ ] Measure the per-call cost of `ProfileRegistry::bundled()` parsing,
      fingerprint validation, and repeated plan-identity validation in the
      installed production path.
- [ ] Cache or processor-own the already validated immutable contract/profile
      identity without weakening unknown-profile, stale-plan, or drift checks.
- [ ] Verify text-only guards as well as image24 t1/t8 so a small serial win is
      not hidden by the image workload.
- [ ] Retain only a measurable non-regressing simplification; otherwise record
      the bound and revert it.

### Final integration

- [ ] Combine only independently retained variants and rerun the full t1/t8
      matrix for both profiles and architectures, including internal
      efficiency, CPU utilization, and transient memory.
- [ ] Run thread-budget output/error invariance, full image conformance,
      installed-wheel checks, repository correctness, and evidence validation.
- [ ] Publish before/after artifacts with exact-source provenance, leave every
      remaining miss explicit, and update the acceptance criteria below.

## Acceptance Criteria — revised by stop decision

The original plan called for detailed architecture-by-architecture profiling.
On 2026-08-26 the King clarified that this pass is intended to find major
implementation mistakes, not pursue individual cache misses or kernel-level
tuning. Detailed x86 hardware-counter attribution and additional medium/large
kernel projects are therefore intentionally out of scope for v0.1.

- [x] Audit the production t8 path for a major scheduling, allocation, cache,
      or work-partitioning defect.
- [x] Measure the four ranked low-risk hypotheses in isolation and document why
      each candidate was rejected when it failed to repeat across profiles.
- [x] Preserve the single bounded eight-worker pool, exact outputs, ordering,
      stable errors, and memory semantics by reverting every non-qualifying
      experiment.
- [x] Use production-path sampling to establish that remaining time is
      predominantly real JPEG/resize/patchify work rather than overlooked setup
      overhead.
- [x] Pass full repository correctness, conformance, evidence, and installed-
      wheel verification on the restored clean implementation.
- [x] Leave no production source change and record the explicit decision to
      stop rather than undertake disproportionate x86 counter/SIMD/pipelining
      work before v0.1.

## Worklog

### 2026-08-24 — created from compact certification miss

The exact-source `5a77e78c85e0ef11da154d573fb405abb9c9780b` result
measured Qwen3-VL/Qwen3.5 `image24/t8` efficiencies of `0.5105/0.5188` on
ARM and `0.2573/0.2570` on x86, all below the frozen `0.6` gate. This ticket
owns those four failed gate IDs without changing the thread budget or
threshold. See `benchmarks/performance-certification-v1/result.json` and
`report.md`.

### 2026-08-26 — re-scoped as the single multithreaded optimization pass

The King selected one focused multithreaded pass before v0.1 rather than making
the former 60% efficiency threshold a release blocker. x86 is the priority at
roughly 26% efficiency; ARM is secondary at roughly 51% because its absolute
t8 result is already about 6.4x faster than official. The stop rule is:
profile both architectures, try only ranked low-risk changes, keep repeatable
wins with no t1/correctness/memory regression, publish the final efficiency,
and stop when those opportunities are exhausted.
- [2026-08-26 12:13] [codex:fd99c9f9] — ### 2026-08-26 — code-path audit and ranked hypotheses

  Read the current production Python/core path against the published D4 evidence. The strongest first experiment is the retained-output allocation boundary: `crates/qwen-mm-python/src/output.rs::run_batch_internal` completes parallel media planning, then `allocate_batch_destinations` serially reserves and default-initializes every destination before `execute_batch_plan` re-enters the owned pool. `pixel_values` dominates the image24 retained output (453,429,240/288 total retained bytes by profile), and `allocate::<f32>` uses `Vec::resize(..., 0.0)`, so the complete float output is written once serially before parallel patchify overwrites it. The historical observed lane measured `binding.destination.allocate` at 13.38 ms ARM and 45.99 ms x86 for qwen3-vl image24; current D4 t1/t8 Amdahl residuals are about 20.8–21.1 ms ARM and 141.6–141.7 ms x86. Those residuals are not attribution by themselves, but the code boundary and mandatory 453 MB first touch make allocation/initialization the leading cause to instrument on the current x86 host. First retainable experiment should keep the same owned pool and compare bounded pool-parallel initialization; an uninitialized-destination API could remove the redundant write but is higher risk and must prove every element is initialized before exposure on success and never exposed on error.

  Second-ranked target is per-occurrence pic-scale setup in `resize_pic_scale_u8`: every image constructs the same Bicubic/PreferQuality/Single plan and allocates/zeroes fresh destination and scratch storage. `image24` repeats one geometry 24 times, while pic-scale returns a shared `Arc<Resampling + Send + Sync>`. Measure batch-local immutable plan reuse keyed by source/destination geometry and separately measure bounded per-worker scratch reuse; preserve single-threaded resampling, stable error precedence, exact counters, and the total eight-worker budget.

  Third-ranked target is final patchify scheduling. The writer must emit roughly 453 MB of float32 output, uses a scalar stride-3 RGB gather followed by temporal-plane `copy_within`, and the parallel splitter bisects by image count rather than output rows. Profile per-stage utilization before changing the kernel. Weighted splitting is relevant to ragged workloads but will not explain uniform image24; intra-image splitting is a fallback only if current utilization shows leaf granularity. Do not retry the normalization LUT: D3 already rejected it after a controlled 35–39% x86 stage regression. The recent temporal-plane reuse is already retained.

  Small serial cleanup: `ProfileRegistry::bundled()` reparses and revalidates the embedded JSON in both batch planning and plan-identity validation on every call. Cache/store the validated contract identity in the processor only after the three larger boundaries are measured. Python media copies remain lower priority for encoded image24, and D3 already rejected borrowed zero-copy because writable providers could mutate during the GIL-free window.
- [2026-08-26 12:41] [codex:fd99c9f9] — Experiment 1 (ARM image24, real paired harness, 15 measured samples/profile after 3 warmups): fallible alloc_zeroed destination buffers preserved outputs and MemoryError behavior but was not repeatable. t8 qwen3-vl 41.122->38.884 ms while qwen3.5 39.803->42.459 ms; t1 qwen3-vl 142.548->140.880 ms while qwen3.5 141.012->143.631 ms. Rejected due sign flip/noise; restoring baseline allocator.
- [2026-08-26 12:48] [codex:fd99c9f9] — Experiment 2 (ARM image24/t8): implemented operation-bounded per-Rayon-worker pic-scale plan/scratch reuse via map_init and compared both paths from the same native binary using a temporary control switch. Reuse measured 33.195/33.233 ms (qwen3-vl/qwen3.5); original path measured 32.884/33.211 ms under the same host state. The earlier ~41 ms control had host drift, confirmed by the official adapter shifting ~280->244 ms. Rejected: no benefit (vl ~0.9% slower, qwen3.5 flat), extra cross-layer lifecycle complexity; reverting.
- [2026-08-26 12:55] [codex:fd99c9f9] — Experiment 3 (ARM image24/t8) rejected after two same-binary A/Bs. Scheduling: 8 coarse three-image leaves vs original 24 one-image leaves measured 33.679/34.077 ms vs 33.699/33.658 ms (qwen3-vl/qwen3.5), flat with sign flip. Kernel: provably disjoint copy_from_slice for temporal planes vs existing copy_within measured 33.174/33.527 ms vs 33.488/33.517 ms, a ~0.31 ms vl-only shift and flat qwen3.5 despite identical geometry/code. Patchify/conformance tests passed, but neither change is repeatable across profiles; reverting both and their control switches.
- [2026-08-26 13:00] [codex:fd99c9f9] — Experiment 4 (ARM image24/t8) rejected: process-wide cached bundled registry/identity vs original per-call parse, selected by same-binary control. Cached measured 41.375/39.287 ms; uncached 37.688/38.256 ms (qwen3-vl/qwen3.5), while official timings also shifted ~3 ms, so there is no repeatable gain. This agrees with 725c's observed t1 attribution of only ~0.10 ms total planning+identity parse work. Reverting cache and control switch rather than retain persistent state for noise-level work.
- [2026-08-26 13:03] [codex:fd99c9f9] — ARM four-experiment checkpoint complete. All experimental source and runtime control switches are reverted; clean source rebuilt into the active .venv. make check passed end-to-end: Ruff, rustfmt, Clippy -D warnings, 97 core + 5 Python native unit tests (plus examples/docs), 133 reference tests, Phase-C v2 45/45, golden/corpus conformance, offline core build, and isolated installed-wheel smoke. No production source diff remains; only the f49c ticket/worklog is modified. Ticket stays in_progress because x86-first measurements, CPU/utilization attribution, and any cross-host final evidence remain outstanding.
- [2026-08-26 14:29] [codex:fd99c9f9] — Native production-path ARM sampling (8 threads, 8 s at 1 ms, repeated image24) attributes main-thread wall phases: plan_batch wait 3180/5371 samples (59.2%), execute_plan_into wait 1529/5371 (28.5%), and serial output __bzero 588/5371 (10.9%). Collapsed worker leaf stacks are led by patchify (10248 samples), then JPEG decode/IDCT/upsample plus pic-scale NEON convolution; plan/scratch setup is small. Conclusion: the four rejected setup-level experiments missed the dominant kernel work. A precise x86 perf/hardware-counter run is the remaining high-value diagnostic; likely next implementations are medium/large (safe uninitialized output, explicit patchify SIMD, or fused/pipelined media+patchify), not another cheap cache/granularity tweak.
- [2026-08-26 16:48] [codex:fd99c9f9] — Stop decision: the bounded pass was looking for major implementation mistakes, not individual cache misses. Four isolated low-risk experiments found no repeatable win, production-path sampling found remaining time in real JPEG/resize/patchify kernels, all candidate code was reverted, and the full restored implementation passed make check. Per the King, skip detailed x86 hardware-counter profiling and larger SIMD/pipelining projects for v0.1.
- [16:48] [codex:fd99c9f9755e8c3a] — Closed: Bounded major-defect optimization pass complete; no qualifying production change, and further kernel-level profiling is intentionally out of scope for v0.1.

## Lifecycle

- 2026-08-26T20:48:42Z [codex:fd99c9f9755e8c3a] — closed (completed): Bounded major-defect optimization pass complete; no qualifying production change, and further kernel-level profiling is intentionally out of scope for v0.1.
