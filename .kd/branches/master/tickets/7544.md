---
id: "7544"
status: closed
deps: [5fb8, 6eea, 70c3]
links: []
created: 2026-08-26T13:33:45Z
type: task
priority: 2
closed_at: 2026-09-23T15:26:19Z
resolution: completed
close_reason: "Implementation verified previously; user explicitly authorized opening and publishing on 2026-09-23, superseding separate personal-review hold. No personal API review is inferred."
closed_context: codex:09044ae762a8387c
parent: 57e6
---
# Final v0.1 usability and Rosetta Stone approval gate

## Goal

After the focused usability implementation tickets close, rewrite the Rosetta
Stone against the finished public API and make the remaining delta from the
official examples as small and unsurprising as possible. The implementation and installed-example verification are complete. The King
explicitly directed opening and publishing the package on 2026-09-23; that newer
instruction supersedes the separate personal-review hold. No new personal API
review is claimed.

## Implementation tickets

- `5fb8` — familiar `Processor.from_pretrained` construction using the
  canonical short names `Qwen3` and `Qwen3.5` or exact Hugging Face model IDs.
- `6eea` — direct local-path, URL, data-URI, and OpenAI-style image sources.
- `70c3` — concise single-request preparation, left-padding compatibility, and
  mapping-style prepared outputs.

## Scope

- Inventory every official Qwen3-VL and Qwen3.5 code sample relevant to the
  declared v0.1 text/still-image envelope, recording its authoritative source
  and pinned version or revision.
- Include the official Hugging Face model and documentation pages,
  `qwen-vl-utils`, vLLM, and SGLang examples, plus relevant Qwen-owned
  repositories, cookbooks, and multimodal plugins discovered during the
  inventory. Include key downstream Qwen consumers such as LLaMA-Factory and
  ModelScope SWIFT where their dataset or request schemas reveal additional
  usability requirements. Cover text-only, single-image,
  multi-image/interleaved, and video examples even when video is classified as
  deferred rather than supported.
- Classify each sample as supported in v0.1, intentionally unsupported, or
  deferred to video/vLLM; omissions must be explicit rather than accidental.
- Write the shortest clear installed-wheel qwen-mm equivalent for each
  supported flow: text-only, one image, multiple/interleaved images, mixed
  formats and shapes, batched requests, raw RGB, options, outputs, and errors.
- Make the primary review document a focused Rosetta Stone: for each distinct
  official flow, show the small relevant upstream preprocessing excerpt and
  the exact qwen-mm equivalent immediately after it. Keep source-audit detail
  and broad classifications in a secondary inventory rather than forcing a
  reader through them before seeing the code comparison.
- Review constructor inputs, request schema, naming, return values, metadata,
  thread controls, exceptions, snapshot discovery, and failure messages for
  accidental complexity.
- Simplify the API or add thin ergonomic helpers where the comparison exposes
  real friction, without opaque callbacks, unrestricted fallback, or a second
  competing request model. Filesystem and network I/O are triggered by the
  explicit model or media value supplied by the caller.
- Add a familiar `Processor.from_pretrained(model)` happy path. It accepts
  the canonical short names `Qwen3` and `Qwen3.5` plus the exact
  supported Hugging Face model IDs, resolves them to qwen-mm's tested profile
  and pinned revision, and downloads through the normal Hugging Face cache when
  the snapshot is absent. Preserve an explicit offline/cache-only option and
  keep `from_huggingface_cache` as the lower-level reproducible path.
- Accept caller-supplied local paths and HTTP(S) image URLs directly in public
  image content, normalizing them to the existing owned-byte/index model before
  native preprocessing. Cover both the Qwen `{"type": "image", "image": ...}`
  form and the Qwen3.5 OpenAI-style `image_url` form. If the caller supplies an
  HTTP(S) URL, qwen-mm fetches it directly: there is no opt-in permission flag,
  private-network filtering, security policy, or warning ceremony. Ordinary
  resource ceilings and actionable network errors remain reliability behavior,
  not a gate on the caller's explicit request.
- Rewrite the Rosetta Stone only after `5fb8`, `6eea`, and `70c3` close. Preserve
  the official messages verbatim wherever the flow is supported and make the
  qwen-mm side differ only where its preprocessing/output boundary genuinely
  differs.
- Make the root README the user entrypoint with installation, a one-screen
  quickstart, supported/unsupported matrix, threading guidance, and links to
  deeper examples.
- Turn published examples into installed-wheel tests so documentation cannot
  drift from the actual interface.

## Non-goals

Do not add encoded video, arbitrary PIL/tensor compatibility, a vLLM adapter,
unrestricted model/revision fallback, or generic Hugging Face processor
emulation merely to make an official sample appear supported. Model downloads
remain limited to known compatibility profiles and pinned revisions. Explicit
image URLs are not subjected to a library security policy. Other unsupported
examples are documented and deferred honestly.

## Acceptance Criteria

- [x] A versioned comparison table covers every relevant official sample for
      both pinned profiles and identifies supported, unsupported, and deferred
      behavior with no unexplained gap.
- [x] `5fb8`, `6eea`, and `70c3` are closed with their installed-wheel tests and
      full repository verification passing.
- [x] `Processor.from_pretrained` accepts `Qwen3`, `Qwen3.5`, and both
      canonical supported model IDs, downloads only pinned processor artifacts,
      and supports an offline/cache-only mode.
- [x] Qwen-style direct path/URL images and Qwen3.5-style `image_url` content
      normalize to the existing deterministic byte/index core with bounded I/O.
- [x] The Rosetta Stone is rewritten against the final API; supported qwen-mm
      examples retain the official message objects and are materially similar
      in construction and call-site complexity.
- [x] Every supported comparison has a concise, runnable qwen-mm equivalent
      exercised from a freshly installed wheel on the declared platforms.
- [x] The happy path—from import and processor construction through prepared
      arrays—is understandable without reading implementation or benchmark
      code, and common errors tell users how to correct the request.
- [x] Root documentation states the exact v0.1 scope, model/snapshot
      prerequisites, input forms, output contract, threading behavior, and
      performance-claim boundaries.
- [x] Full conformance, binding/ownership, wheel, and documentation-example
      tests pass after any ergonomic changes.
- [x] An opt-in installed-release-wheel test feeds Qwen3.5 arrays into pinned
      local Qwen3.5-0.8B weights and verifies text, image, mixed-batch, logits,
      and generation behavior without treating inference latency as a gate.
- [x] Publication is authorized by the King’s 2026-09-23 instruction to open,
      push, and set up publishing. This supersedes the earlier separate personal
      API review hold; it does not assert that a personal review occurred.

## Worklog

### 2026-08-26 — created as the main v0.1 product gate

The King made usability the decisive release blocker: inspect the code and API
against all relevant official examples, then make the result simple, clean,
and clear. Performance optimization continues separately; a fast processor
with a confusing interface is not a releasable v0.1.

### 2026-08-26 — source inventory expanded and work started

The King explicitly named Hugging Face pages, `qwen-vl-utils`, vLLM, and
SGLang as example sources and required a mixture of text, image, multi-image,
and video flows. The inventory will also search Qwen-owned repositories,
cookbooks, and multimodal plugins. Video remains in the comparison so its
deferred status is visible; this does not expand the v0.1 implementation scope.

### 2026-08-26 — downstream ecosystem sources added

The King added LLaMA-Factory as a key comparison repository. The inventory now
also covers comparable Qwen-heavy training stacks such as ModelScope SWIFT when
they expose distinct dataset, media-ordering, or batching schemas. Training,
dataset loading, and fine-tuning remain outside the wheel; these sources test
whether the preprocessing boundary is usable for real downstream callers.
- [2026-08-26 17:22] [codex:ad56e22c] — Pinned and reviewed the public preprocessing examples at Qwen3-VL 96588727, Qwen3.5 model revision c2022362, Transformers 5.14.1/a08ace4b, qwen-vl-utils 0.0.14, vLLM 0.23.0/0fc695fc, SGLang 2935bb8e, Qwen-MM-Plugins 1.0.8/ab339d20, LLaMA-Factory a18110d2, and ModelScope SWIFT 5e198be8. The distinct flows are text, one image, interleaved/multiple images, heterogeneous batches, raw RGB, pixel controls, thinking/tools, and video/path/URL/frame-list inputs. LLaMA-Factory and SWIFT confirm that ordered message placeholders plus separate media lists are an important downstream shape; training and dataset loading remain outside v0.1.
- [2026-08-26 17:22] [codex:ad56e22c] — Usability implementation in progress: added a no-network Processor.from_huggingface_cache classmethod, supported_profiles discovery, model_id/revision properties, actionable missing-snapshot errors, a root install/quickstart/scope guide, a pinned official-example comparison, and fresh-wheel documentation examples. Targeted Rust clippy and Python lint pass. The documentation examples pass against the installed editable wheel. The full installed binding script currently reaches a pre-existing 24-image pixel hash mismatch (actual c752cd89..., committed golden 45fa4c11...); the changed constructor code is not on that processing path, so this is being recorded and isolated rather than masking it.
- [2026-08-26 17:26] [codex:ad56e22c] — Confirmed the image24 binding failure against a clean HEAD wheel: clean HEAD produced the same c752cd89 pixel hash while the immutable v1 Pillow golden stores 45fa4c11. Since docs/image-resize-contract-v2.md explicitly replaced bitwise Pillow equality for resized still images, the binding/ownership test now keeps exact keys, shapes, grids, text arrays, size, dtype, layout, and lifetime checks while leaving resized-pixel quality to the versioned resize-v2 conformance gate. The fresh-wheel documentation and full binding suites now both pass.
- [2026-08-26 17:29] [codex:ad56e22c] — Verification complete for the usability changes: make check passed (ruff, rustfmt, workspace Clippy/tests/doctests, 133 reference tests, fixture/golden/corpus checks, conformance smoke, and fresh wheel smoke); make python-binding-test passed both the fresh-wheel public documentation examples and full binding/ownership suite; and make phase-c-v2-validate passed the current qwen-mm-still-image-resize-v2 overlay. The superseded Phase B v1 direct runner still reports expected exact-Pillow pixel mismatches under the accepted v2 resize amendment and is not the current still-image numerical contract. All 7544 criteria are satisfied except the explicitly human-only King approval.
- [2026-08-26 17:30] [codex:ad56e22c] — Ready for King review. The proposed public path is Processor.from_huggingface_cache(profile, thread_budget=...), followed by one prepare_batch call over explicit messages plus separate caller-owned media buffers. Review focus: constructor and discovery naming, numeric media references, arrays/metadata split, deliberate no-I/O and no-video boundaries, and the coverage classifications in docs/official-example-comparison-v0.1.md.
- [2026-08-26 17:30] [codex:ad56e22c] — Implementation files: README.md is the public entrypoint; docs/official-example-comparison-v0.1.md is the versioned inventory and boundary matrix; crates/qwen-mm-python/src/lib.rs contains the additive cache/profile helpers; crates/qwen-mm-python/tests/docs_examples.py is the public-flow suite; binding.py and smoke.py cover discovery and the corrected resize-v2 ownership boundary; scripts/test-python-binding.sh runs documentation examples before the full installed-wheel suite. This ticket and the release epic state are committed with those changes while the unrelated concurrent 9516 worktree edit remains unstaged.
- [2026-08-26 17:58] [codex:ad56e22c] — King review requested a more focused primary deliverable: a Rosetta Stone that places a small official preprocessing sample immediately beside the exact qwen-mm equivalent. The exhaustive source inventory remains useful as an audit appendix, but it is not the review entrypoint. Ticket returned to active work; the concise side-by-side document and its runnable examples will be added before requesting approval again.
- [2026-08-26 18:09] [codex:ad56e22c] — Added docs/official-example-rosetta-stone-v0.1.md as the primary code-first review path. It pairs pinned official snippets immediately with qwen-mm equivalents for construction, text, URL image loading, multi-image, heterogeneous batching, qwen-vl-utils explicit sizing, Qwen3.5 non-thinking image input, LLaMA-Factory repeated-image rows, video rejection, and the vLLM/SGLang model-runner boundary. README now links the Rosetta Stone first; the exhaustive inventory remains the appendix.
- [2026-08-26 18:09] [codex:ad56e22c] — Fresh-wheel Rosetta tests now mirror the documented prompts and media shapes, including 280x420 explicit sizing, Qwen3.5 image preprocessing with enable_thinking false, a repeated-image LLaMA-Factory conversation, and encoded video rejection. Verification passed: make python-binding-test, make check, make phase-c-v2-validate, ruff formatting/checks, Python syntax compilation for all 18 documented snippets, and git diff --check.
- [2026-08-27 08:42] [codex:ad56e22c] — King review rejected the cache-only/bytes-only happy path as too disruptive relative to official Qwen APIs. New direction: add a familiar Processor.from_pretrained entrypoint accepting qwen3/qwen3.5 aliases and exact supported Hugging Face model IDs, auto-populate the normal cache at qwen-mm's pinned revision, and accept direct local image paths and HTTP(S) image URLs in message content. Keep the deterministic preloaded-byte/index form and explicit offline controls for services and reproducible runs. Ticket returned to active implementation; the Rosetta Stone must become materially closer to the official samples before review resumes.
- [2026-08-27 08:45] [codex:ad56e22c] — Design inspection: the native parser currently requires numeric image references plus preloaded bytes/RGB, the wheel depends only on NumPy, and the core already enforces 64 MiB encoded-item and 1 GiB encoded-batch ceilings. A compatibility facade can keep the Rust core deterministic: use huggingface_hub.snapshot_download with allow_patterns restricted to the eight hash-pinned processor/tokenizer artifacts (not model weights), then normalize explicit Path/file/http/https/data-URI and OpenAI image_url inputs to the existing owned-byte/index request before entering native code. Remote reads must stream against the encoded-byte cap rather than read an unbounded response first.
- [2026-08-27 08:51] [codex:ad56e22c] — King clarified the product decision: explicit image URLs are ordinary supported inputs. qwen-mm must fetch a caller-supplied HTTP(S) URL directly, with no permission switch, private-network filtering, security mode, or warning ceremony. Resource ceilings, timeouts, and network errors are reliability mechanics only. Canonical short model names are capitalized Qwen3 and Qwen3.5. Split implementation into 5fb8 (construction and pinned downloads), 6eea (direct image sources), and 70c3 (single-request, padding, and output ergonomics). This ticket is now the final gate: it depends on all three, then requires a rewritten, materially closer Rosetta Stone and explicit King approval.
- [2026-08-27 08:57] [codex:ad56e22c] — King requested parallel native-subagent implementation. Delegated one bounded worker to each ready dependency: 5fb8 for model construction and pinned downloads, 6eea for direct path/URL/data-URI image sources, and 70c3 for single-request, left-padding, and prepared-output ergonomics. All work in the shared tree without commits or ticket closure; each must leave targeted evidence and move to in_review. The owning session will inspect every diff, integrate overlaps, run the full repository/binding/resize suites, close the dependencies only after review, then activate 7544 for the final Rosetta Stone rewrite and King approval.
- [2026-08-27 09:26] [codex:ad56e22c] — Root reviewed the three delegated slices as one public API, reran the combined fresh-wheel and repository gates, integrated the README, closed 5fb8/6eea/70c3, and committed the implementation as d7d0e35. The reviewed behavior is explicit: `Processor.from_pretrained` accepts canonical `Qwen3`/`Qwen3.5` names or exact supported model IDs and downloads only pinned processor artifacts; direct caller-supplied HTTP(S) images fetch without permission switches, private-address filtering, security modes, or warning ceremony; `prepare` handles one official messages list; `prepare_batch` supports explicit left padding; and `PreparedBatch` is a mapping over official NumPy arrays while metadata stays separate.
- [2026-08-27 09:26] [codex:ad56e22c] — Rewrote the Rosetta Stone against the finished API. Text, URL image, multi-image, qwen-vl-utils sizing, and Qwen3.5 OpenAI `image_url` examples now reuse the official `messages` unchanged and reduce the qwen-mm side to one `prepare` call. The heterogeneous batch retains only the per-request wrappers needed for distinct rows and requests `padding_side="left"`; LLaMA-Factory requires only its genuine dataset-placeholder expansion and may pass original path lists; video remains explicitly deferred. Updated the exhaustive comparison appendix so it no longer claims caller-owned path/URL loading or fixed right padding.
- [2026-08-27 09:26] [codex:ad56e22c] — Final verification passed after the Rosetta rewrite: `make python-binding-test` built a fresh wheel and passed pretrained construction, runnable documentation, direct media, usability/padding/mapping, and ownership suites; `make check` passed Ruff formatting/lint (including Markdown Python fences), rustfmt, Clippy with warnings denied, 124 Rust tests (97 passed and 27 snapshot-gated ignored in core; 5 passed and 4 embedded-Python-gated ignored in the binding), doc tests, all 133 reference tests, fixture/golden/corpus/conformance validation, and a fresh wheel smoke; `make phase-c-v2-validate` passed; and `git diff --check` passed. All criteria are satisfied except the explicitly human-only King approval.
- [2026-09-05 14:25] [codex:398f29ad] — Reopened the current-state assessment at the King's request: compare Phase D epic 9516, the final Rosetta Stone, implementation, and measured performance to identify the remaining path to a fast, usable release. This is a review of the existing release work, not approval of the API or a release. Existing uncommitted edits in 57e6 and 9516 are preserved.
- [2026-09-05 14:27] [codex:398f29ad] — Current-state review findings (2026-09-05): HEAD 954dbca implements the Rosetta Stone's from_pretrained, direct image sources, prepare, left-padded prepare_batch, and mapping outputs. The remaining meaningful consumer gap is the NumPy-to-model handoff: usability.py:107 tests **prepared against a dummy function, not a real Qwen consumer; public examples stop before device transfer, generation, and output decoding. Recommend a short optional Transformers recipe plus a real consumer smoke for both pinned profiles, preserving the framework-independent NumPy core. This is a recommendation, not a new approved public API or expansion into a model runner.

  Performance assessment: historical D4 source 5a77e78 records 107/122 PASS and 15 MISS, with image24/t8 p50 about 37.8 ms / 6.4x on ARM and 166.7-167.0 ms / 3.48-3.63x on x86. Later 725c source f75e1d6 records ARM rgb24/t1 around 50.4-50.7 ms / 1.80x in diagnostic paired evidence. Neither is final-candidate certification after the public facade changes. f49c intentionally closed with no retained change; detailed x86 kernel profiling was explicitly stopped. Do not reopen speculative optimization to gate v0.1. Native worker budget defaults to one, while headline batches use eight. Direct file/URL loading is synchronous and sequential in _media.py and is outside the historical in-memory CPU claims.

  Recommended release sequence within 57e6: complete and test the consumer recipe; finish e8d1 license/metadata/changelog/native release automation and clean-install rehearsal; measure exact final shipping wheels on ARM/x86, preserving the original MISS and separating native CPU, source-loading, and consumer handoff timing; present the concrete candidate for the already-required human API/release approval. Keep post-v0.1 strict memory/noise work in 9516. Current CI has two-platform Rust checks but only a Linux development-wheel smoke; python-binding-test builds with test-hooks and without --release, so final production wheels need their own public-example/conformance checks. Package metadata restricts Python to 3.11 and pins NumPy/HF Hub exactly; verify consumer installation compatibility and document the narrow support matrix. Root BENCHMARK.md still leads with the old official-only baseline and should link prominently to measured native results. Resize-v2 fidelity is the declared numerical contract, not bitwise Pillow identity or proof of unchanged model quality.
- [2026-09-05 14:27] [codex:398f29ad] — Review verification: make python-binding-test completed successfully on local macOS ARM from the current checkout. It built and installed a fresh wheel and passed pretrained-construction, documentation/Rosetta examples, media-source, usability/padding/mapping, and binding/ownership suites. Log: /tmp/qwen-mm-shipping-review-binding.log. No production code changed; this review did not rerun the full repository suite, Linux release validation, controlled performance capture, or real-model inference. Findings are recorded here; human approval remains unchecked.
- [2026-09-05 14:40] [codex:398f29ad] — The King supplied an OpenRouter credential and authorized small Qwen3.5 API smoke requests within a couple-of-dollars budget; this run uses a $2 ceiling. Credential is stored outside the repository with owner-only permissions and will not be logged. Remote text/image smoke tests will use synthetic inputs only and are explicitly separate from qwen-mm tensor-consumer verification.
- [2026-09-05 14:40] [codex:398f29ad] — OpenRouter smoke completed successfully: qwen/qwen3.5-9b via SiliconFlow passed 3/3 small requests with reasoning disabled and a 128-token output cap. Text returned QWEN_OK (1.277 s), synthetic red PNG returned red (2.015 s), and ordered synthetic red/blue PNGs returned red,blue (1.123 s). Total provider-reported cost was $0.00003045. Results contain no credentials and are saved at /tmp/qwen-mm-openrouter-smoke.json. This establishes remote API access and text/single-image/multi-image request behavior only; no local qwen-mm preprocessing tensor or latency claim follows from it.
- [2026-09-05 14:45] [codex:398f29ad] — The King authorized downloading Qwen3.5-0.8B locally and using Transformers on this Mac to verify the actual processor-to-model contract; inference latency is irrelevant to acceptance. Plan: pin the small model revision, compare its tokenizer/chat/image contract with qwen-mm's existing Qwen3.5-9B profile, then run official and native text/image/batch inputs through the same local model. Treat the 0.8B weights as a verification consumer without silently advertising a new supported processor profile.
- [2026-09-05 14:46] [codex:398f29ad] — Pinned local consumer to Qwen/Qwen3.5-0.8B revision 2fc06364715b967f1860aea9cf38778875588b17 (1,746,942,600-byte safetensors weight file). Downloaded metadata proves tokenizer.json, vocab.json, merges.txt, and image/video preprocessor configs are byte-identical to the pinned 9B profile. tokenizer_config differs only in its embedded chat template; the standalone template differs only in default thinking behavior. Explicit enable_thinking=False (or True) aligns the branch. Vision patch_size=16, temporal_patch_size=2, spatial_merge_size=2 and input channels=3 match. The consumer smoke will set thinking explicitly and retain this compatibility witness.
- [2026-09-05 14:52] [codex:398f29ad] — First real-model run passed all five initial cases against a freshly installed --release wheel without test hooks: text, single image, multi-image, heterogeneous left-padded batch, and explicit resize. Non-resized cases had byte-exact model inputs, exact first-token logits, and identical eight-token greedy generations. Resized patterned pixels differed by at most 0.07843137 in normalized space and first-token logits by 0.2361486; both models generated successfully and happened to produce identical token sequences. That resized token agreement is diagnostic, not a replacement for resize-v2 fidelity or a general quality claim. Individual CPU generations took about 8.5-9.4 seconds and two-row batches about 17 seconds. Adding a non-resized patterned image to strengthen layout coverage and testing the complete reusable isolated-wheel runner next.
- [2026-09-05 14:55] [codex:398f29ad] — Final local consumer verification passed 6/6 cases via sh scripts/test-transformers-consumer.sh, which built a production --release wheel without test-hooks and installed it with hash-locked reference dependencies into a fresh temporary environment. The added aligned patterned image also produced exact inputs, first-token logits, and greedy token IDs. All five non-resized cases were exact; the resized case had finite logits and successful generation on both paths, with identical generated tokens observed but not required. Retained report: reference/consumer/v1/macos-arm64.json. Its script SHA-256 matches the final script, and it records pinned processor/model identities, model weight hash, wheel hash, package versions, shapes, input/logit differences, outputs, and diagnostic CPU timings.

  Added scripts/verify_transformers_consumer.py, scripts/test-transformers-consumer.sh, and docs/transformers-consumer-verification.md; linked the reproducible model handoff from README and the Rosetta Stone. Model weights remain in the ignored HF cache. Verification passed: full make check (lint/format, Rust/Clippy/tests/doctests, all 133 reference tests, fixture/golden/corpus/conformance checks, isolated wheel smoke), make phase-c-v2-validate, shell syntax, and consumer report/script provenance. The first make check attempt found only formatting in the new Markdown Python example; fixed it and the complete rerun passed. No production processing code or public model support changed. Returning 7544 to in_review with the real-consumer criterion satisfied; existing human API/release approval remains unchecked.
- [2026-09-05 14:59] [codex:398f29ad] — Rosetta follow-up audit at the King's request found two reproducible gaps in the exact documented forms, using the installed release wheel and both pinned official processors. For an unchanged 128x128 PNG message, direct AutoProcessor.apply_chat_template(tokenize=True, return_dict=True, return_tensors=np) produces image_grid_thw [[1,16,16]], while qwen-mm prepare produces [[1,8,8]]. Token array shapes are (1,78) vs (1,30) for Qwen3 and (1,82) vs (1,34) for Qwen3.5 with thinking explicitly disabled. Root cause: the frozen contract records image_config_min_pixels=65536 but composed_image_min_pixels=4096, and geometry.rs uses the composed value. The existing local consumer smoke deliberately uses the composed qwen-vl-utils oracle and 256px no-resize fixtures; its PASS remains valid for that path but does not establish direct-Transformers default equivalence claimed by the Rosetta translations.

  Second gap: the original type=video/video=URL content fails with InvalidRequestError about content item.input_index expecting an integer, for both profiles. The Rosetta and docs_examples.py replace it with a numeric reference plus encoded bytes before checking UnsupportedMediaError, so they miss the original message's misleading rejection. The public boundary should reject the original video form explicitly without trying to fetch it.

  Recommended next work (proposed, not an approved public API redesign): establish one executable case inventory keyed to every Rosetta section, and render/extract the published qwen-mm snippets rather than maintaining approximate handwritten mirrors; compare the actual direct-Transformers and composed-qwen-vl-utils source calls separately; resolve the conflicting image-budget defaults explicitly rather than silently changing the frozen core; cover original URL/path/data-URI/OpenAI forms, multi-turn tools/thinking, repeated-media dataset rows, heterogeneous padding, and deferred video errors. Expand local consumer checks to those exact supported flows with native tensors; use OpenRouter only for real hosted request/media/tool behavior with explicit transport mappings, not local tensor parity or equality with the different 0.8B model. Retain deterministic both-profile preprocessing checks and distinguish them from the Qwen3.5-only local model witness.

  The principal remaining API friction is per-row wrappers for ordinary conversation batches, explicit NumPy-to-Torch bridging, and needing an official processor for output decoding. Consider common batch options/bare conversation lists, optional return_tensors=pt, and decode/batch_decode forwarding as narrowly scoped convenience additions; keep the current single prepare call and supported exact model IDs. Compare each proposed addition against the same examples before deciding. Full matrix then needs exact production-wheel execution on ARM and Linux x86 under e8d1. Returned this gate to in_progress because the newly demonstrated exact-example discrepancies remain unresolved; no further paid calls or production API edits occurred during this audit.
- [2026-09-05 15:03] [codex:398f29ad] — The King authorized the complete proposed Rosetta verification and ergonomics program. Implementation choices: retain the frozen composed-image default (4096 minimum pixels), expose common min_pixels/max_pixels keyword defaults so direct Transformers examples can state min_pixels=65536 explicitly, preserve per-image overrides, and document the unavoidable upstream-default difference. Add ordinary conversation-list batches with shared chat options, optional return_tensors=pt with device transfer, and decode/batch_decode through the existing pinned native tokenizer. Native NumPy output and existing request-wrapper semantics remain the default contract. Fix original video URL/frame-list rejection before media I/O. Execute the actual Markdown snippets for every section and retain explicit handling for transport/dataset/deferred boundaries. Extend local and hosted checks, then validate native macOS and Linux wheels. These conveniences are now within authorized scope, superseding the earlier non-goal against direct Torch output.
- [2026-09-05 15:20] [codex:398f29ad] — Exact Markdown execution now passes both-profile preprocessing. Original upstream multi-image/batch file: URLs fail in pinned Transformers 5.14.1 itself; preserved the excerpts, documented its plain-path adjustment, and record that reference-only fixture adjustment explicitly. qwen-mm executes the original file: messages. Regression checks first demonstrated missing shared min_pixels and premature image I/O before video rejection, then passed with fixes. Pinned Qwen BPE decoding ignores cleanup=True; a failing Unicode/punctuation/special-token oracle test caught the initial generic cleanup implementation, which has been removed to match the actual pinned tokenizer. No mandatory Torch dependency added; optional Torch output shares native NumPy storage and model examples now use native batch_decode.
- [2026-09-05 15:24] [codex:398f29ad] — Fresh ARM production wheel passed the exact Markdown suite with real Qwen3.5-0.8B generation for all nine supported Qwen3.5 sections, including the two-row batch, tool/thinking history, raw RGB, and native output decoding. All generated-fixture inputs, first-token logits, and generated IDs matched the official route exactly. Both profiles passed preprocessing and expected original-video rejection. OpenRouter passed seven additional calls (text, data-URI single/ordered multi-image, unchanged section 6 public image message with documented transport mappings, tool call/result, thinking enabled). Hosted image-location content is not graded for accuracy; its 128-token output intentionally truncated. Reports retained under reference/rosetta/v1. Finishing native Linux, live URL preprocessing, full checks, and a fresh installed-wheel resize-quality capture without relabeling historical Phase C/D source evidence.
- [2026-09-05 15:29] [codex:398f29ad] — Final API review caught shared min/max defaults creating forbidden inline-plus-nested image options. Added and ran the nested-options regression against the installed wheel (failed InvalidRequestError), then fixed default merging to preserve the existing nested form and per-image override precedence without mutating inputs. Rebuilding both platform witnesses after this facade-only correction. First Linux production-wheel Rosetta pass and original-public-URL preprocessing pass on both profiles are complete. Fresh ARM installed-wheel resize capture passed all 43 public holdout geometries under the unchanged full resize-v2 fidelity metrics; the two non-public geometries remain explicitly historical core evidence. OpenRouter's seven-call cost was $0.00036235, plus the earlier $0.00003045, for $0.00039280 total.
- [2026-09-05 15:34] [codex:398f29ad] — Full fresh legacy Phase C v1 execution completed on both the pre-change production wheel and current production wheel: all 290 result records are exactly identical. Both have 270 PASS and 20 FAIL, entirely the pre-existing resized-pixel differences and additive left_padding metadata in old golden request layouts. This is explicitly not a v1 PASS. Retained both complete reports in reference/rosetta/v1/legacy-conformance-comparison.zip plus a comparison JSON; no golden or threshold changed. Final ARM/Linux installed-wheel resize evidence passes all 43 public geometries; native wheel source Python files match the checkout exactly. Full make check, final python-binding-test, and historical phase-c-v2-validate pass. Report review found uv omits local-wheel hashes from direct_url.json, so the executable suite now hashes the actual installed wheel source file itself and is being rerun to bind each Rosetta witness to the matching resize capture wheel hash.
- [2026-09-05 15:36] [codex:398f29ad] — Final evidence review passed: Rosetta report document/script hashes match the final files; ARM and Linux wheel SHA-256 values match their fresh installed-wheel resize captures; both wheel archives contain exactly the current Python facade sources. ARM: 25 inventory entries with 20 array comparisons across both profiles, two expected video errors, constructors/serving boundary/generation status, and real generation for all nine Qwen3.5 flows. Linux: same preprocessing/error inventory from its native production wheel; model inference is explicitly the ARM witness. Original public URLs pass on both profiles. Both architecture resize witnesses pass all 43 public geometries. Hosted report provenance is current; seven new calls cost $0.00036235 and session total is $0.00039280. Full make check, final python-binding-test, historical phase-c-v2-validate, pre-commit hooks, shell syntax, and diff whitespace checks pass. CI now runs the default exact-snippet release-wheel suite on Linux x86_64 and macOS ARM without paid calls or model-weight downloads.

  Implementation and reproducible evidence are complete for this authorized Rosetta program. docs/rosetta-verification.md links all reports and states the unchanged legacy v1 mismatches, source/default adjustments, local-versus-hosted boundaries, and no Qwen3-VL inference or renewed performance claim. Returning 7544 to in_review, with the final Rosetta criterion satisfied and human-only API/release approval still unchecked. Release packaging and exact shipping performance certification remain owned by e8d1/57e6; no publication or approval is inferred. The unrelated pre-existing edit to ticket 9516 is preserved.
- [11:26] [codex:09044ae762a8387c] — Closed: Implementation verified previously; user explicitly authorized opening and publishing on 2026-09-23, superseding separate personal-review hold. No personal API review is inferred.

## Lifecycle

- 2026-09-23T15:26:19Z [codex:09044ae762a8387c] — closed (completed): Implementation verified previously; user explicitly authorized opening and publishing on 2026-09-23, superseding separate personal-review hold. No personal API review is inferred.
