---
id: "9e1d"
status: closed
deps: [08a0, 9580, 72e4]
links: [9516]
created: 2026-08-02T12:28:37Z
type: epic
priority: 2
closed_at: 2026-08-02T16:04:54Z
---
# Phase C: Batch and Python parity release

## Goal

Turn the conformant single-request Rust image processor from Phase B into a
native heterogeneous batch operation, expose that operation through a safe
PyO3/NumPy boundary, and pass the complete pinned text/image compatibility
corpus for both supported profiles. Phase C is the first Python-facing parity
release; it establishes correctness and ownership semantics before profiling or
performance claims begin.

## Child Tickets

- C1 / `08a0` — implement the two-phase serial batch API.
- C2 / `9580` — build the PyO3/NumPy binding.
- C3 / `72e4` — pass and publish the full image conformance gate.

## Execution Shape

C1 starts from the closed A6, B1, and B7 contracts. C2 follows C1 so the Python
surface wraps a proven core ownership and capacity model. C3 follows C2 and the
closed A3 corpus so it validates the actual Python-facing release boundary. The
epic depends on all three children and is a completion roll-up, not executable
implementation work.

## Scope Boundaries

Phase C remains serial and correctness-first. Deterministic parallelism,
profile-driven fusion, performance certification, video support, and production
vLLM integration belong to Phases D through F. Benchmark results may be gathered
for diagnostics, but no speed claim is allowed before C3 closes.

## Acceptance Criteria

- [x] C1 through C3 are closed with implementation and verification evidence in
      their worklogs.
- [x] The Rust core exposes planning, caller-destination execution, and
      allocating batch APIs for heterogeneous requests with deterministic,
      atomic results.
- [x] One Python call prepares a complete batch while the GIL is released and
      without a Python callback per visual; official arrays have exact public
      dtypes, shapes, strides, and safe ownership.
- [x] Both frozen profiles pass the complete declared text/image corpus through
      the Python-facing path, including conditional keys and stable error
      categories, and the first conformance report is published reproducibly.
- [x] No Phase C artifact claims certified speed, parallelism, video parity, or
      production vLLM readiness; downstream work remains gated by the roadmap.
- [x] The full repository correctness suite passes from a clean checkout, and
      `ROADMAP.md` is updated only if Phase C evidence changes downstream scope
      or dependencies.

## Worklog

### 2026-08-02 — Phase C completed

Closed C1 at `83b5d2b`, C2 at `ee1a932`, and C3 with implementation/fix
commits `5b9dddf` and `0364b5a`. Published the passing versioned Phase C report
and summary at `9206f5e`.

The final clean `make phase-c-release-check` passed the full repository suite,
Phase B, installed-wheel smoke/binding verification, and all 290 declared Phase
C candidate cases for both profiles with zero failures or skips. A second clean
run also passed and reproduced the authenticated source fingerprint, exact
results, assets, comparator, rule map, runtime identities, and readable
summary. Standalone validation passes after the evidence commit.

The release remains serial and correctness-only; the report contains no speed
claim and the benchmark guard remains unreleasable before Phase D's dedicated
host criteria. `ROADMAP.md` was not changed because the evidence did not alter
downstream scope or dependencies.

### 2026-08-02 — epic created

Created the Phase C epic and its three roadmap children. Dependencies preserve
the roadmap graph: C1 depends on A6/B1/B7, C2 depends on C1, C3 depends on
A3/C2, and the epic depends on all three children as a completion roll-up.
